---
phase: 06-instadecks-content-review
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - tools/build-cross-domain-fixture.js
  - tests/fixtures/cross-domain-test-deck.pptx
  - tests/fixtures/cross-domain-design-findings.json
  - tests/fixtures/cross-domain-content-findings.json
  - tests/content-vs-design-boundary.test.js
autonomous: true
requirements: [CRV-10]

must_haves:
  truths:
    - "tools/build-cross-domain-fixture.js authors `tests/fixtures/cross-domain-test-deck.pptx` deterministically via pptxgenjs 4.0.1 (pinned exact, locked invariant) per D-05 fixture spec: 4 slides — slide 1 title; slide 2 BOTH visual (#CCCCCC body on #FFFFFF — fails WCAG <3:1) AND content (vague unsupported claim) defects; slide 3 visual-only (off-grid bullet by ~12pt) with clean content; slide 4 content-only (30-word run-on bullet with 7 acronyms) with clean visuals."
    - "Build script is idempotent: same Node version + same pptxgenjs version → byte-identical PPTX (no Date.now / Math.random / per-run UUIDs); reproducibility documented in script header comment."
    - "Build script invocation: `node tools/build-cross-domain-fixture.js` (no flags) writes to fixed path; CI may regenerate as a sanity check; PPTX is committed (avoids requiring CI to run pptxgenjs)."
    - "Pre-computed findings fixtures committed: `cross-domain-design-findings.json` (DECK-VDA findings against the deck — visual defects only, slides 2 & 3) and `cross-domain-content-findings.json` (8-check findings — content defects only, slides 2 & 4); both authored by hand per the spec, validated by Phase 1 schema-validator v1.1."
    - "Boundary regression test (`tests/content-vs-design-boundary.test.js`) asserts BIDIRECTIONALLY: (a) cross-domain-design-findings.json contains zero findings on slide 4, AND every finding has `category` ∈ {defect, improvement, style} (NEVER 'content'); (b) cross-domain-content-findings.json contains zero findings on slide 3, AND every finding has `category === 'content'` AND a valid `check_id`; (c) both fixtures pass schema-validator v1.1 without throwing."
    - "Test does NOT call any LLM — operates on pre-computed JSON fixtures so CI is deterministic + offline (per §'Check D-05 — Boundary regression test' of 06-RESEARCH.md)."
    - "Test additionally asserts cross-finding-set invariants: (1) intersection of slideNums in design-findings ∩ content-findings is exactly {2} (the both-defects slide); (2) slide 1 has zero findings in either set; (3) slide 4 has zero design findings AND ≥1 content finding (one each for length + jargon at minimum)."
    - "Both findings fixtures, when run through the Phase 2 /annotate adapter (live require — not mocked), produce SAMPLES arrays that round-trip without throwing — proves Plan 06-01's adapter patch works on real content findings (not just synthetic ones)."
  artifacts:
    - path: "tools/build-cross-domain-fixture.js"
      provides: "Deterministic pptxgenjs author of the boundary-test deck (Q-4 / D-05)"
      contains: "pptxgenjs"
    - path: "tests/fixtures/cross-domain-test-deck.pptx"
      provides: "Committed binary fixture with 4 calibrated slides (visual + content defect mix)"
      contains: "(binary)"
    - path: "tests/fixtures/cross-domain-design-findings.json"
      provides: "Hand-authored DECK-VDA findings — visual-only, schema v1.1, slides 2+3"
      contains: "category"
    - path: "tests/fixtures/cross-domain-content-findings.json"
      provides: "Hand-authored 8-check findings — content-only, schema v1.1, slides 2+4"
      contains: "check_id"
    - path: "tests/content-vs-design-boundary.test.js"
      provides: "Bidirectional regression test locking CLAUDE.md content-vs-design invariant into CI (CRV-10)"
      contains: "category"
  key_links:
    - from: "tests/content-vs-design-boundary.test.js"
      to: "skills/review/scripts/lib/schema-validator.js"
      via: "validates BOTH findings JSONs against v1.1 schema"
      pattern: "validate"
    - from: "tests/content-vs-design-boundary.test.js"
      to: "skills/annotate/scripts/adapter.js"
      via: "round-trips both fixtures through adapter to confirm Plan 06-01 patch"
      pattern: "toSamples"
    - from: "tools/build-cross-domain-fixture.js"
      to: "tests/fixtures/cross-domain-test-deck.pptx"
      via: "node tools/build-cross-domain-fixture.js writes the committed binary"
      pattern: "writeFile"
---

<objective>
Wave 3 (parallel-safe with 06-04): build the cross-domain boundary fixture (Q-4 / D-05) and the bidirectional regression test that locks CLAUDE.md's content-vs-design invariant (CRV-10) into CI. Authors a deterministic 4-slide PPTX via pptxgenjs (one slide each: clean / both defects / visual-only / content-only), commits hand-authored findings JSONs for both `/review` and `/content-review` against that deck, and asserts bidirectionally that neither skill's findings cross the boundary.

Purpose: CRV-10 is the only success criterion not covered by Plans 06-01/02. Without this test the boundary is prose discipline only — a CI regression test makes a future boundary leak fail loud (Pitfall 3). Test runs on pre-computed JSON (no LLM in CI loop) → deterministic, offline-friendly, fast.

Output: 1 build tool + 1 PPTX fixture + 2 findings JSON fixtures + 1 test file. Test green; both fixtures pass schema-validator v1.1; both round-trip through the /annotate adapter without throwing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-instadecks-content-review/06-CONTEXT.md
@.planning/phases/06-instadecks-content-review/06-RESEARCH.md
@.planning/phases/06-instadecks-content-review/06-01-PLAN.md
@.planning/phases/06-instadecks-content-review/06-02-PLAN.md
@skills/review/scripts/lib/schema-validator.js
@skills/annotate/scripts/adapter.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author cross-domain fixture deck via pptxgenjs</name>
  <files>tools/build-cross-domain-fixture.js, tests/fixtures/cross-domain-test-deck.pptx</files>
  <action>
    Per §"Check D-05 — Boundary regression test" of 06-RESEARCH.md verbatim. Build a Node script using `pptxgenjs` (version 4.0.1 — pinned exact per CLAUDE.md locked invariant; do NOT bump). Four slides:
      - Slide 1: title slide ("Q4 Strategic Review" + author line) — clean visual, clean content, slide_type === 'title'.
      - Slide 2: BOTH defects — body text color #CCCCCC on background #FFFFFF (fails WCAG contrast <3:1), title 'Solution Overview' (a topic label, not a claim — Major content defect via title-check), single bullet 'Our solution is innovative and disruptive in the market.' (vague unsupported claim — Major prompt-side claim/evidence finding).
      - Slide 3: visual-only — title 'Revenue grew 40% in Q3 from enterprise renewals' (clean action title), bullet 'Q3 ARR: $4.2M, sourced from 2026-04 board pack' (sourced evidence), but bullet position offset ~12pt off-grid (visual defect that DECK-VDA flags); content is clean.
      - Slide 4: content-only — title 'Strategic Direction' (topic label — Major content defect), bullet of exactly 30 words containing 7 distinct unfiltered acronyms ('Our SaaS B2B SMB GTM motion via PLG drives ARR ACV expansion through ICP-aligned MQLs that convert to SQLs at industry-leading rates per the latest BCG report' — Major content defect via length AND jargon checks); visual is clean (proper contrast, on-grid).
    Determinism: do NOT include `creator`, `company`, `subject` metadata that varies per run; if pptxgenjs sets timestamp metadata by default, override with a fixed ISO date (e.g., `2026-04-28T00:00:00Z`). Script header comment documents reproducibility recipe ("node $VERSION + pptxgenjs 4.0.1 → byte-identical output"). Run the script, verify output, commit the PPTX as a binary asset.
  </action>
  <verify>
    <automated>node tools/build-cross-domain-fixture.js && test -f tests/fixtures/cross-domain-test-deck.pptx && tools/lint-paths.sh</automated>
  </verify>
  <done>build-cross-domain-fixture.js runs cleanly; PPTX file exists at expected path; lint-paths gate passes; PPTX opens in LibreOffice with the four expected slides (manual smoke optional, not required for green).</done>
</task>

<task type="auto">
  <name>Task 2: Author paired findings JSON fixtures (design + content)</name>
  <files>tests/fixtures/cross-domain-design-findings.json, tests/fixtures/cross-domain-content-findings.json</files>
  <action>
    Hand-author two findings JSON files, both honoring schema v1.1. (1) `cross-domain-design-findings.json` — the findings a hypothetical DECK-VDA pass would emit:
      - 1 finding on slide 2: low-contrast body text. category:'style', severity_reviewer:'Major', genuine:true, nx:0.5, ny:0.6, no check_id (not a content finding), text in Phase 3 grammar.
      - 1 finding on slide 3: off-grid bullet alignment. category:'defect', severity_reviewer:'Minor', genuine:true.
      - ZERO findings on slide 4 (boundary check — even though slide 4 has obvious content issues, /review must not cross the boundary).
      - ZERO findings on slide 1.
    (2) `cross-domain-content-findings.json` — the findings the 8-check pass would emit:
      - Slide 2: ≥2 findings — one for action-title (check_id:'action-title', Major, blocked-phrase 'Overview') AND one prompt-side claim/evidence (check_id:'claim-evidence', Major).
      - Slide 4: ≥3 findings — action-title (Major), length (Major, >25 words), jargon (Major, 7 acronyms).
      - ZERO findings on slide 3 (boundary check — clean content).
      - ZERO findings on slide 1.
      - All findings: category:'content', valid check_id, severity_reviewer ∈ {Critical,Major,Minor,Nitpick}.
    Both JSONs include the schema_version field set to '1.1' and pass schema-validator v1.1 without throwing.
  </action>
  <verify>
    <automated>node -e "const {validate}=require('./skills/review/scripts/lib/schema-validator'); validate(require('./tests/fixtures/cross-domain-design-findings.json')); validate(require('./tests/fixtures/cross-domain-content-findings.json')); console.log('OK');"</automated>
  </verify>
  <done>Both fixtures committed; both pass v1.1 validator; manual review confirms slideNum coverage matches the boundary spec.</done>
</task>

<task type="auto">
  <name>Task 3: Bidirectional boundary regression test</name>
  <files>tests/content-vs-design-boundary.test.js</files>
  <action>
    node:test + node:assert/strict. Implement the assertions from must_haves.truths exactly:
      (a) design fixture: zero findings on slide 4; every finding's category ∈ {defect, improvement, style} and category !== 'content'.
      (b) content fixture: zero findings on slide 3; every finding's category === 'content'; every finding has a valid check_id from the 8-value enum.
      (c) Both fixtures pass the v1.1 validator.
      (d) Cross-set invariants: design ∩ content slideNums === {2}; slide 1 has zero findings in either; slide 4 has zero design findings AND ≥1 content finding for each of length+jargon check_ids.
      (e) Round-trip through `/annotate` adapter live (not mocked): for each fixture, call `require('skills/annotate/scripts/adapter').toSamples(findings)` (or whichever export is the canonical entry — confirm by reading adapter.js); assert no throw and assert resulting SAMPLES contain entries with severity in {major, minor, polish} (the 4→3 collapse) — this proves Plan 06-01's adapter patch works on real content findings.
    Test must NOT make any LLM call, must NOT spawn child processes, must NOT touch network. Pure JSON-in / asserts-out. Includes a comment block citing CLAUDE.md content-vs-design boundary invariant + 06-RESEARCH.md §"Check D-05" so future maintainers don't loosen the assertions.
  </action>
  <verify>
    <automated>npm test -- tests/content-vs-design-boundary.test.js</automated>
  </verify>
  <done>Test green; bidirectional assertions cover all five invariant categories (a-e); adapter round-trip proves Plan 06-01's patch holds; comment block cites the locked invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-cross-domain-fixture.js → committed PPTX | Build-time determinism boundary; non-deterministic output undermines test reproducibility |
| fixture JSONs → adapter live require | Crossing boundary asserts Plan 06-01's adapter patch holds end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | Boundary test loosened over time | mitigate | Comment block in test cites CLAUDE.md locked invariant + RESEARCH §D-05; PR template requires double-sign-off to weaken these assertions |
| T-06-10 | Repudiation | Fixture findings hand-authored without provenance | accept | Both fixtures are tagged with the spec they implement (D-05); regenerable from build-cross-domain-fixture.js + the spec table in 06-RESEARCH.md |
</threat_model>

<verification>
- `npm test -- tests/content-vs-design-boundary.test.js` green
- Both fixture JSONs pass `validate()` standalone
- `git status` shows committed PPTX, both JSONs, build script, test file
- `tools/lint-paths.sh` passes
- pptxgenjs version unchanged in package.json (4.0.1 pinned exact, locked invariant)
</verification>

<success_criteria>
SC#3 (content-vs-design boundary preserved bidirectionally, verified by golden test fixtures with cross-domain content) — fully covered. CRV-10 fully covered. No other CRV requirements addressed in this plan (intentional — single-concern scope).
</success_criteria>

<output>
After completion, create `.planning/phases/06-instadecks-content-review/06-03-SUMMARY.md`
</output>

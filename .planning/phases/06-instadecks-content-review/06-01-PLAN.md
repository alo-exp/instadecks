---
phase: 06-instadecks-content-review
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - skills/review/references/findings-schema.md
  - skills/review/scripts/lib/schema-validator.js
  - skills/annotate/scripts/adapter.js
  - skills/content-review/scripts/lib/extract-content.js
  - skills/content-review/scripts/lib/title-adapter.js
  - skills/content-review/scripts/lib/redundancy.js
  - skills/content-review/scripts/lib/jargon.js
  - skills/content-review/scripts/lib/length-check.js
  - tests/findings-schema-v11.test.js
  - tests/annotate-adapter-content-category.test.js
  - tests/content-review-extract.test.js
  - tests/content-review-checks.test.js
  - tests/fixtures/content-review/sample-extract.json
autonomous: true
requirements: [CRV-04, CRV-06, CRV-07, CRV-09]

must_haves:
  truths:
    - "findings-schema.md header bumped to `Schema version: 1.1`; `category` enum extended to {defect,improvement,style,content}; OPTIONAL `check_id` row added with enum {action-title,redundancy,jargon,length,pyramid-mece,narrative-arc,claim-evidence,standalone-readability}; `check_id` is REQUIRED iff `category === 'content'` (D-03 / CRV-09)."
    - "schema-validator.js extends CATEGORIES Set to include `content`; rejects `category:'content'` findings missing `check_id` or with `check_id` outside the 8-value VALID_CHECK_IDS Set; existing v1.0 fixtures still validate (no regression on tests/findings-schema.test.js / tests/review-schema-emission.test.js)."
    - "annotate/scripts/adapter.js VALID_CATEGORY Set extended to include `content` (Pitfall 1 / Q-3 patch); SEV_MAP unchanged (4→3 collapse: Critical/Major→major(orange), Minor→minor(blue), Nitpick→polish(grey)); content findings flow through adapter unmodified."
    - "lib/extract-content.js exports async `extractContent(pptxPath)` returning `{slides:[{slideNum,title,bullets[],body,notes,sources[],slide_type}]}`; uses jszip via `skills/review/scripts/lib/read-deck-xml.js::loadSlides`; groups <a:r><a:t> runs by <a:p> parent (Pitfall 4); infers slide_type ∈ {title,section,content,closing}; deterministic — no Date.now / Math.random / network."
    - "lib/title-adapter.js exports `checkTitles(extract)` -> findings[]; wraps `skills/create/scripts/lib/title-check.js::validateTitle` (REUSE; no modification per CONTEXT.md D-02 / Phase 4 D-06); maps `validateTitle` reasons to Major (blocked phrase) / Minor (too short / no verb) per spec; closing-slide override via `{action_title_override: slide.slide_type==='closing'}`; emits findings with `category:'content'`, `check_id:'action-title'`, `nx:0.5`, `ny:0.08`, `genuine:true`, full grammar text + Minto standard + concrete fix."
    - "lib/redundancy.js exports `checkRedundancy(extract)`; bag-of-words cosine on title + first bullet; tokens lowercase, strip punctuation `[^\\p{L}\\p{N}\\s]`, drop length≤2, drop 25-word stop-word set per spec; whitelists boilerplate titles {agenda,q&a,thank you,questions,appendix,table of contents}; skips slides with `slide_type ∈ {section,closing,title}`; for each unordered (i,j) pair, if cos≥0.85 emit finding on max(i,j) referencing min(i,j); severity Major if cos≥0.95 else Minor; `category:'content'`, `check_id:'redundancy'`, `nx:0.5`, `ny:0.5`, MECE standard, merge/differentiate fix."
    - "lib/jargon.js exports `checkJargon(slide)` -> findings[]; counts distinct UPPERCASE 2-5-char tokens via /\\b[A-Z]{2,5}\\b/g in `body + bullets.join(' ')`; filter-list {I,II,III,IV,V,OK,USA,EU,UK,CEO,CTO,CFO}; emit when distinct count >5; severity Major if ≥8 else Minor; `category:'content'`, `check_id:'jargon'`, `nx:0.5`, `ny:0.6`, audience-fit (Knaflic 2015) standard."
    - "lib/length-check.js exports `checkLength(slide)` -> findings[]; whitespace-token count per bullet; emit one finding per bullet >25 words; severity Major if >35 else Minor; `category:'content'`, `check_id:'length'`, `nx:0.5`, `ny: clamp(0.3 + 0.1*bulletIndex, 0.3, 0.85)`, 6×6 / Reynolds 2008 standard."
    - "All 4 code-side checks emit `severity_reviewer` in {Critical,Major,Minor,Nitpick} (title-case, never UPPERCASE per Pitfall 2); each unit-test asserts producer-side severity vocabulary."
    - "Every test file uses node:test + node:assert/strict; zero new npm deps; lint-paths gate passes (no /Users/, ~/.claude/, hardcoded absolute paths)."
  artifacts:
    - path: "skills/review/references/findings-schema.md"
      provides: "Locked findings schema bumped 1.0→1.1 (additive: content category + check_id)"
      contains: "Schema version: 1.1"
    - path: "skills/review/scripts/lib/schema-validator.js"
      provides: "Validator accepting v1.1 (content + check_id) without regressing v1.0 fixtures"
      contains: "VALID_CHECK_IDS"
    - path: "skills/annotate/scripts/adapter.js"
      provides: "Phase 2 adapter patched to accept category:'content' (Q-3 / Pitfall 1)"
      contains: "'content'"
    - path: "skills/content-review/scripts/lib/extract-content.js"
      provides: "PPTX → {slides:[{slideNum,title,bullets,body,notes,sources,slide_type}]} via jszip"
      contains: "extractContent"
    - path: "skills/content-review/scripts/lib/title-adapter.js"
      provides: "checkTitles wrapper around skills/create/scripts/lib/title-check.js (REUSE, never modify)"
      contains: "checkTitles"
    - path: "skills/content-review/scripts/lib/redundancy.js"
      provides: "Cross-slide bag-of-words cosine ≥0.85 redundancy check (CRV-06)"
      contains: "checkRedundancy"
    - path: "skills/content-review/scripts/lib/jargon.js"
      provides: "Per-slide UPPERCASE 2-5-char acronym counter (CRV-07)"
      contains: "checkJargon"
    - path: "skills/content-review/scripts/lib/length-check.js"
      provides: "Per-bullet >25-word length check (CRV-07)"
      contains: "checkLength"
    - path: "tests/fixtures/content-review/sample-extract.json"
      provides: "Canonical 6-slide extract object exercising every code-side check positive + negative"
      contains: "slide_type"
  key_links:
    - from: "skills/content-review/scripts/lib/title-adapter.js"
      to: "skills/create/scripts/lib/title-check.js"
      via: "require + validateTitle({action_title_override})"
      pattern: "validateTitle"
    - from: "skills/content-review/scripts/lib/extract-content.js"
      to: "skills/review/scripts/lib/read-deck-xml.js"
      via: "loadSlides(pptxPath) jszip read"
      pattern: "loadSlides"
    - from: "skills/review/scripts/lib/schema-validator.js"
      to: "skills/review/references/findings-schema.md"
      via: "VALID_CHECK_IDS Set mirrors §3 enum"
      pattern: "check_id"
    - from: "skills/annotate/scripts/adapter.js"
      to: "skills/review/scripts/lib/schema-validator.js"
      via: "VALID_CATEGORY enum mirror (must stay in lockstep)"
      pattern: "VALID_CATEGORY"
---

<objective>
Wave 1 (parallel-safe foundation): bump findings schema 1.0 → 1.1 (additive: `category:'content'` enum value + optional `check_id`), patch the schema-validator and the `/annotate` adapter to accept the new enum value (Q-3 / Pitfall 1), and ship the four code-side content checks (`title-adapter`, `redundancy`, `jargon`, `length-check`) plus the jszip-based PPTX text extractor (`extract-content`). All work TDD red→green; zero new npm deps; reuses Phase 4 `title-check.js` verbatim and Phase 3 `read-deck-xml.js::loadSlides` verbatim.

Purpose: All four code-side checks (CRV-04 / CRV-06 / CRV-07) plus the schema-bump backbone for CRV-09 land here so Plan 06-02's `runContentReview` orchestrator has every deterministic primitive on disk and validated. Schema + adapter patch land in the SAME plan to prevent the integration-time failure flagged in Pitfall 1.

Output: 8 source files green (1 schema doc + 2 patches + 5 new libs); 4 test files green (~30 subtests); 1 fixture committed; zero deps added; existing Phase 1/2/3 tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-instadecks-content-review/06-CONTEXT.md
@.planning/phases/06-instadecks-content-review/06-RESEARCH.md
@skills/review/references/findings-schema.md
@skills/review/scripts/lib/schema-validator.js
@skills/annotate/scripts/adapter.js
@skills/review/scripts/lib/read-deck-xml.js
@skills/create/scripts/lib/title-check.js

<interfaces>
<!-- Reused verbatim — DO NOT MODIFY -->
From skills/create/scripts/lib/title-check.js:
```js
function validateTitle(title, opts = {}) {
  // returns { ok: boolean, reason?: string }
  // reason ∈ { "blocked phrase: <phrase>", "too short (need ≥3 words...)", "no verb detected ..." }
  // opts.action_title_override === true skips check (returns {ok:true})
}
module.exports = { validateTitle };
```

From skills/review/scripts/lib/read-deck-xml.js:
```js
async function loadSlides(pptxPath) {
  // returns [{ slideNum: number, xml: string }, ...] using jszip 3.10.1
}
module.exports = { loadSlides };
```

From skills/review/scripts/lib/schema-validator.js (current v1.0 shape — to extend):
```js
const CATEGORIES = new Set(['defect', 'improvement', 'style']);  // EXTEND
function validate(findings) { /* throws on invalid */ }
module.exports = { validate, CATEGORIES };
```

From skills/annotate/scripts/adapter.js (current line 7 — to extend):
```js
const VALID_CATEGORY = new Set(['defect', 'improvement', 'style']);  // EXTEND to include 'content'
const SEV_MAP = { Critical: 'major', Major: 'major', Minor: 'minor', Nitpick: 'polish' };  // UNCHANGED
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema bump v1.1 + validator + adapter (RED → GREEN, single atomic landing)</name>
  <files>skills/review/references/findings-schema.md, skills/review/scripts/lib/schema-validator.js, skills/annotate/scripts/adapter.js, tests/findings-schema-v11.test.js, tests/annotate-adapter-content-category.test.js</files>
  <behavior>
    Test 1 (findings-schema-v11.test.js):
      - validate(finding with category:'content', check_id:'redundancy', severity_reviewer:'Major', ...) PASSES
      - validate(finding with category:'content', missing check_id) THROWS with message including "check_id: required for category=\"content\""
      - validate(finding with category:'content', check_id:'unknown-id') THROWS listing the 8 valid ids
      - validate(legacy v1.0 finding with category:'defect', no check_id) PASSES (back-compat)
      - validate(finding with severity_reviewer:'MAJOR') THROWS (P-01 producer-side title-case guard, Pitfall 2)
    Test 2 (annotate-adapter-content-category.test.js):
      - adapter.toSamples([{category:'content', check_id:'jargon', severity_reviewer:'Major', ...}]) returns SAMPLES with severity 'major' (orange) — NO throw
      - adapter accepts category:'content' AND legacy categories simultaneously in one findings doc
      - SEV_MAP collapse for content findings: Critical→major, Major→major, Minor→minor, Nitpick→polish
  </behavior>
  <action>
    Per D-03 / Q-3: (1) Edit findings-schema.md — bump header to `Schema version: 1.1`; in §3 Finding shape extend `category` enum to {defect,improvement,style,content}; add OPTIONAL `check_id` row with enum of 8 values (action-title, redundancy, jargon, length, pyramid-mece, narrative-arc, claim-evidence, standalone-readability); §6 Schema version policy clarification: "1.x validators tolerate optional new fields; required-iff rules expressed inline." (2) Patch schema-validator.js: extend CATEGORIES Set; add `const VALID_CHECK_IDS = new Set([...8 ids...])`; after existing required-field loop add the `if (f.category === 'content') { ... }` branch from §"Schema Compatibility" of 06-RESEARCH.md verbatim. (3) Patch annotate/scripts/adapter.js line 7: VALID_CATEGORY Set adds `'content'`. NO other adapter changes — SEV_MAP stays. (4) Write both tests RED first, run, then make them GREEN. Verify existing tests/findings-schema.test.js + tests/review-schema-emission.test.js still pass (back-compat). Atomic commit: `feat(06-01): bump findings schema v1.0→v1.1 + adapter patch (D-03/Q-3)`.
  </action>
  <verify>
    <automated>npm test -- tests/findings-schema-v11.test.js tests/annotate-adapter-content-category.test.js tests/findings-schema.test.js tests/review-schema-emission.test.js</automated>
  </verify>
  <done>All four test files green (new + back-compat); schema header reads `Schema version: 1.1`; validator + adapter patches in place; no new deps in package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PPTX text extractor (extract-content.js) RED→GREEN</name>
  <files>skills/content-review/scripts/lib/extract-content.js, tests/content-review-extract.test.js, tests/fixtures/content-review/sample-extract.json</files>
  <behavior>
    - extractContent(pathToFixturePPTX) resolves to {slides:[{slideNum,title,bullets,body,notes,sources,slide_type}]}
    - Title slide → slide_type === 'title'; section divider → 'section'; content slide → 'content'; closing ('Thank You') → 'closing'
    - Bullets preserve <a:p> hierarchy: a slide with title + 3 bullets returns title in `title` and bullets.length === 3 (Pitfall 4 — does NOT flatten)
    - body === bullets.join(' '); notes pulled from ppt/notesSlides/notesSlideN.xml when present, else ''
    - Accented chars (e.g., "café") survive round-trip
    - Pure: no Date.now / Math.random / network; deterministic across runs
  </behavior>
  <action>
    Implement per §"Code Examples — Example 2" of 06-RESEARCH.md. Reuse `loadSlides` from skills/review/scripts/lib/read-deck-xml.js (do NOT shell out to unzip+xmllint per Pitfall 4 + Anti-Patterns). Group <a:r><a:t> runs by <a:p> parent via regex pass on each slide xml; first <a:p> in title placeholder shape → title; subsequent <a:p> in body shapes → bullets[]. inferSlideType(xml): if shape role contains 'title' AND no body → 'title'; if layout name contains 'section' → 'section'; if title text matches /^(thank you|q&a|questions)$/i → 'closing'; default 'content'. Build a tiny PPTX fixture inline in the test (reuse Phase 4 cookbook output if available, else author via pptxgenjs in test setup) — OR write fixture once and commit as tests/fixtures/content-review/sample-extract.json (the parsed extract object) for downstream check tests in Task 3. Test must include both the round-trip extraction AND a frozen extract JSON fixture other tests reuse. Atomic commit: `feat(06-01): add extract-content.js jszip-based PPTX extractor (D-01)`.
  </action>
  <verify>
    <automated>npm test -- tests/content-review-extract.test.js</automated>
  </verify>
  <done>extract-content.js green; sample-extract.json fixture committed; bullet hierarchy preserved; no shell-out; zero new deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Four code-side checks (title-adapter / redundancy / jargon / length) RED→GREEN</name>
  <files>skills/content-review/scripts/lib/title-adapter.js, skills/content-review/scripts/lib/redundancy.js, skills/content-review/scripts/lib/jargon.js, skills/content-review/scripts/lib/length-check.js, tests/content-review-checks.test.js</files>
  <behavior>
    title-adapter (CRV-04):
      - Slide title "Q3 Revenue" → Minor finding (no verb / too short) check_id:'action-title'
      - Slide title "Overview" → Major finding (blocked phrase)
      - Slide title "Revenue grew 40% in Q3 from enterprise renewals" → no finding
      - slide.slide_type==='closing' → no finding (override)
    redundancy (CRV-06):
      - Two slides with title+first-bullet sharing 9/10 tokens → Major finding on later slide (cos≥0.95)
      - Two slides sharing 8/10 → Minor finding (0.85 ≤ cos < 0.95)
      - Two slides sharing 5/10 → no finding (cos < 0.85)
      - Title 'Agenda' OR slide_type ∈ {section,closing,title} → skipped
    jargon (CRV-07):
      - Slide body with 6 distinct unfiltered acronyms → Minor finding
      - Slide body with 8+ → Major finding
      - 'CEO', 'USA', 'I', 'IV' filtered out
      - ≤5 → no finding
    length-check (CRV-07):
      - Bullet of 26 words → Minor finding
      - Bullet of 36 words → Major finding
      - Bullet of 25 → no finding
      - Multiple long bullets → multiple findings with ny clamped to [0.3, 0.85]
    All findings:
      - severity_reviewer in {Critical,Major,Minor,Nitpick} (title-case, Pitfall 2)
      - category === 'content', check_id matches the lib
      - genuine === true
      - Pass through schema-validator from Task 1 without throwing
  </behavior>
  <action>
    Implement each lib per §"The Seven (actually Eight) Content Checks — concrete heuristic specs" of 06-RESEARCH.md verbatim — token rules, stop-word list, whitelist, slide-type skip, severity calibration, finding shape (text grammar, standard, fix, nx/ny). title-adapter.js MUST `require('../../create/scripts/lib/title-check')` (REUSE — locked invariant; CONTEXT.md D-02 + Phase 4 D-06; do not modify the source). redundancy.js: hand-rolled bag-of-words + cosine (no NLP dep, per "Don't Hand-Roll" exception in 06-RESEARCH). All checks must integrate with Task-1 schema-validator — final test asserts `validate(allFindings)` does not throw. Atomic commit: `feat(06-01): add 4 code-side content checks (title/redundancy/jargon/length, CRV-04/06/07)`.
  </action>
  <verify>
    <automated>npm test -- tests/content-review-checks.test.js tests/findings-schema-v11.test.js</automated>
  </verify>
  <done>All 4 lib files green; tests cover positive, negative, severity-boundary, and skip cases for each check; every emitted finding passes the v1.1 schema validator; title-check.js unchanged on disk (verify with git diff).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user PPTX → extract-content.js | Untrusted zip archive crosses into Node — entries could include path traversal (`../`) or zip-bombs |
| schema-validator → adapter | Internal trust boundary; v1.1 enum drift between validator and adapter is the Pitfall 1 risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | extract-content.js | mitigate | Reuse Phase 3 `read-deck-xml.js::loadSlides` (already hardened: jszip with explicit slide-path filter `^ppt/slides/slide[0-9]+\.xml$` per Phase 3 Plan 03-03 A4); do NOT extend the path filter in this plan |
| T-06-02 | Denial of Service | extract-content.js (zip-bomb) | accept | jszip 3.10.1 has no documented decompression-bomb fuse; same risk as Phase 3 — accepted under same rationale (developer-controlled input, not server-side) |
| T-06-03 | Information Disclosure | adapter.js enum drift | mitigate | Add tests/annotate-adapter-content-category.test.js asserting validator + adapter accept the same category set; future enum bumps require BOTH files patched |
| T-06-04 | Spoofing | check_id field | mitigate | schema-validator.js enforces VALID_CHECK_IDS Set; unknown ids throw — agent cannot inject novel check_id values that downstream tooling treats as valid |
</threat_model>

<verification>
- `npm test` (full suite) green
- `git diff skills/create/scripts/lib/title-check.js` is empty (REUSE invariant)
- `git diff skills/annotate/scripts/annotate.js` is empty (binary-asset invariant; only adapter.js touched)
- `tools/lint-paths.sh` passes on all new files
- `package.json` + `package-lock.json` unchanged (zero new deps)
</verification>

<success_criteria>
SC#2 (CRV-09 schema/adapter compatibility) is fully met by this plan. SC#1 partial (4 of 8 checks shipped — code side). SC#3 + SC#4 deferred to Plans 06-03 / 06-02. CRV-04, CRV-06, CRV-07, CRV-09 fully covered; CRV-02/03/05/08/10/11 deferred to later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/06-instadecks-content-review/06-01-SUMMARY.md`
</output>

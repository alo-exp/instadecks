---
phase: 06-instadecks-content-review
plan: 04
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - tests/content-review-integration.test.js
  - tests/fixtures/content-review/integration-prompt-findings.json
autonomous: true
requirements: [CRV-01, CRV-09, CRV-11]

must_haves:
  truths:
    - "End-to-end integration test exercises the full /instadecks:content-review pipeline against a real PPTX (the cross-domain fixture from Plan 06-03 if available at test time, OR the Phase 4 cookbook fixture as a fallback): extract → 4 code-side checks → merge with pre-recorded prompt-side findings (mocked LLM, deterministic) → runContentReview → assert outputs."
    - "Code-side path is REAL — no mocking. extract-content.js + title-adapter.js + redundancy.js + jargon.js + length-check.js are all live-required and produce findings against the real PPTX. Prompt-side findings are loaded from `integration-prompt-findings.json` (committed fixture authored in this plan) so the test does not require an LLM in CI."
    - "Test asserts: (1) merged findings doc passes schema-validator v1.1; (2) runContentReview writes the three sibling-of-input outputs at expected paths; (3) JSON output is byte-identical to a committed reference snapshot when re-run (determinism via Plan 06-02 renderer invariants); (4) findingCounts + genuineCount in return value are non-zero (proves real signal flowed through); (5) every finding's category === 'content' AND has a valid check_id; (6) require.cache does NOT contain skills/annotate/scripts/index.js (lazy-require invariant; CRV-11)."
    - "Annotate-pipeline branch test: same flow with `annotate:true` and a stubbed `runAnnotate` (replaces the lazy-required module) — asserts the function is invoked with {deckPath, findings, outDir, runId}, assert require.cache NOW contains the annotate module path, assert the stub's resolved value flows through to runContentReview's `annotated` return field. Does NOT actually run soffice/pdftoppm in CI."
    - "Activation-shape smoke check (lightweight CRV-01 prep — full ≥8/10 measurement deferred to Phase 7 DIST-02): test reads skills/content-review/SKILL.md and asserts the description string contains all six D-06 activation anchors verbatim ('content review', 'argument quality', 'story flow', 'is my deck persuasive', 'Pyramid Principle', 'narrative arc'); description ≤1024 chars; user-invocable: true."
    - "Test runtime <10 seconds total; no network; no soffice; no LLM; no shell-out beyond what extract-content already does internally (none — pure jszip)."
    - "Reference snapshot fixture for the deterministic-output assertion is committed at a stable path; if the snapshot needs updating after a legitimate spec change, the test failure message cites the canonical update procedure ('regenerate via UPDATE_SNAPSHOTS=1 npm test')."
  artifacts:
    - path: "tests/content-review-integration.test.js"
      provides: "End-to-end pipeline regression test (extract → checks → merge → runContentReview → /annotate stub)"
      contains: "runContentReview"
    - path: "tests/fixtures/content-review/integration-prompt-findings.json"
      provides: "Pre-recorded prompt-side findings (mocked LLM output) so CI is deterministic + offline"
      contains: "pyramid-mece"
  key_links:
    - from: "tests/content-review-integration.test.js"
      to: "skills/content-review/scripts/index.js"
      via: "live-require runContentReview; full pipeline invocation"
      pattern: "runContentReview"
    - from: "tests/content-review-integration.test.js"
      to: "skills/content-review/scripts/lib/extract-content.js"
      via: "real PPTX text extraction (no mock)"
      pattern: "extractContent"
    - from: "tests/content-review-integration.test.js"
      to: "skills/annotate/scripts/index.js"
      via: "stubbed via require.cache injection BEFORE runContentReview's lazy require fires"
      pattern: "require\\.cache"
---

<objective>
Wave 3 (parallel-safe with 06-03): the end-to-end integration test that proves the full `/instadecks:content-review` pipeline produces valid findings and routes correctly into `/annotate`. Real code-side checks; mocked prompt-side findings (pre-recorded JSON, no LLM); stubbed `runAnnotate` for the --annotate branch (no soffice/pdftoppm). Includes a lightweight activation-shape smoke check anchoring CRV-01.

Purpose: Plans 06-01/02/03 each cover slices of the pipeline in isolation; this plan asserts they compose correctly. Catches integration-time regressions (e.g., a future refactor that breaks the merged findings doc, the lazy-require invariant, or the deterministic output contract). Bounded scope: no LLM, no soffice, no network — runs in <10s on CI.

Output: 1 integration test file + 1 prompt-findings fixture. Test green; deterministic snapshot stable; lazy-require invariant verified end-to-end; activation-anchor smoke check green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-instadecks-content-review/06-CONTEXT.md
@.planning/phases/06-instadecks-content-review/06-RESEARCH.md
@.planning/phases/06-instadecks-content-review/06-01-PLAN.md
@.planning/phases/06-instadecks-content-review/06-02-PLAN.md
@.planning/phases/06-instadecks-content-review/06-03-PLAN.md
@skills/content-review/scripts/index.js
@skills/content-review/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author pre-recorded prompt-side findings fixture</name>
  <files>tests/fixtures/content-review/integration-prompt-findings.json</files>
  <action>
    Hand-author 4-6 prompt-side findings honoring v1.1 schema. Cover all four prompt-side check_ids:
      - 1 finding with check_id:'pyramid-mece' (slideNum:null systemic, severity Major)
      - 1 finding with check_id:'narrative-arc' (slideNum:null, severity Critical — "no resolution")
      - 1 finding with check_id:'claim-evidence' (slideNum:2, severity Major)
      - 1 finding with check_id:'standalone-readability' (slideNum:4, severity Minor)
    All findings: category:'content', genuine:true, severity_reviewer in title-case, full grammar text + standard + fix per the prompt-template specs in 06-RESEARCH.md §"Check 5/6/7/8". Pass schema-validator v1.1 standalone.
  </action>
  <verify>
    <automated>node -e "const {validate}=require('./skills/review/scripts/lib/schema-validator'); validate(require('./tests/fixtures/content-review/integration-prompt-findings.json')); console.log('OK');"</automated>
  </verify>
  <done>Fixture committed; passes v1.1 validator; covers all 4 prompt-side check_ids.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test (real code-side, mocked prompt + annotate)</name>
  <files>tests/content-review-integration.test.js</files>
  <action>
    Use node:test + node:assert/strict. Test suite structure:

    test('full pipeline standalone mode (annotate:false)'):
      1. Resolve fixture deck path: prefer tests/fixtures/cross-domain-test-deck.pptx if it exists (Plan 06-03), else fall back to a Phase 4 cookbook fixture; document the fallback in a comment.
      2. Live-require extractContent + the four code-side check libs; run them on the real PPTX → codeFindings[].
      3. Load prompt-side findings from integration-prompt-findings.json → promptFindings[].
      4. Merge into findingsDoc with schema_version:'1.1' and findings:[...codeFindings, ...promptFindings].
      5. Validate via schema-validator v1.1 (must pass).
      6. Live-call runContentReview({deckPath, runId:'test-int-06-04', outDir:tmpDir, findings:findingsDoc, mode:'standalone', annotate:false}).
      7. Assert: jsonPath + mdPath + narrativePath returned; JSON file at jsonPath round-trips deep-equal with findingsDoc; MD file exists; findingCounts/genuineCount > 0; annotated === null.
      8. Assert require.cache has NO key matching /annotate\/scripts\/index\.js$/ (lazy-require invariant; CRV-11).
      9. Re-run runContentReview with same inputs → byte-identical MD output (determinism).

    test('full pipeline with --annotate branch'):
      1. Pre-populate require.cache for skills/annotate/scripts with a stub `{runAnnotate: async ({deckPath, findings, outDir, runId}) => ({annotatedPptx: '/tmp/stub.pptx', annotatedPdf: '/tmp/stub.pdf'})}`.
      2. Run runContentReview with annotate:true.
      3. Assert stub was invoked with the expected argument shape; `annotated` return field === stub's resolved value.
      4. Assert require.cache NOW contains the annotate module key (proves lazy require fired).

    test('SKILL.md activation-anchor smoke (CRV-01 lightweight)'):
      1. Read skills/content-review/SKILL.md.
      2. Parse frontmatter; assert user-invocable === true; description.length ≤ 1024.
      3. Assert description contains all six D-06 anchors: 'content review', 'argument quality', 'story flow', 'is my deck persuasive', 'Pyramid Principle', 'narrative arc'. (Full ≥8/10 panel measurement deferred to Phase 7 DIST-02 — this is a sanity smoke only.)

    Cleanup: tmpDir removed in afterEach; require.cache restored after annotate test (invalidate keys we touched).

    NO network, NO soffice, NO LLM, NO actual annotate.js execution. Test runtime budget <10s.
  </action>
  <verify>
    <automated>npm test -- tests/content-review-integration.test.js</automated>
  </verify>
  <done>All three sub-tests green; lazy-require invariant verified end-to-end; --annotate stub round-trip works; activation anchors present; runtime <10s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test → require.cache | Test mutates module cache; must restore between tests to avoid pollution |
| Mocked prompt findings → live code-side findings → merged doc | Boundary where producer-side severity vocabulary mismatches would surface (Pitfall 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | require.cache pollution between tests | mitigate | beforeEach/afterEach restores any keys the test inserted; document in test comment |
| T-06-12 | Denial of Service | flaky test from real-PPTX extraction | mitigate | Pin to a committed fixture (Plan 06-03 deck or Phase 4 cookbook); never construct deck on the fly in CI |
</threat_model>

<verification>
- `npm test` (full suite) green — Plans 06-01/02/03/04 all green together
- `node tools/validate-manifest.js` green
- `tools/lint-paths.sh` green
- Phase 6 success criteria SC#1-#4 each have at least one test asserting them somewhere across plans 06-01..06-04
</verification>

<success_criteria>
SC#1 (full pipeline produces findings covering all 7/8 checks; activation-anchor smoke present) — covered. SC#2 (same schema → /annotate adapter consumes unmodified) — verified by --annotate stub branch + Plan 06-01's adapter patch. SC#4 (standalone v1, output pipes into /annotate) — verified end-to-end. CRV-01, CRV-09, CRV-11 fully covered. Phase 6 closes with all four SCs and all eleven CRV requirements green across the four plans.
</success_criteria>

<output>
After completion, create `.planning/phases/06-instadecks-content-review/06-04-SUMMARY.md`
</output>

---
phase: 06-instadecks-content-review
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - skills/content-review/scripts/index.js
  - skills/content-review/scripts/cli.js
  - skills/content-review/scripts/render-content-fixed.js
  - skills/content-review/SKILL.md
  - skills/content-review/references/content-checks.md
  - tests/content-review-runtime.test.js
  - tests/content-review-render-fixed.test.js
  - tests/content-review-lazy-annotate.test.js
autonomous: true
requirements: [CRV-01, CRV-02, CRV-03, CRV-05, CRV-08, CRV-09, CRV-11]

must_haves:
  truths:
    - "scripts/index.js exports `runContentReview({deckPath, runId, outDir, mode='standalone', findings, contentExtract, annotate=false})` mirroring Phase 3 `runReview` API; throws on missing deckPath or findings; calls Phase 1 schema-validator (now v1.1) before any IO; writes sibling-of-input outputs `<deck>.content-review.json` + `<deck>.content-review.md` + run-dir mirror under `.planning/instadecks/<run-id>/` (CRV-09)."
    - "runContentReview lazy-requires `runAnnotate` ONLY inside `if (annotate)` branch (P-07 invariant; Anti-Patterns; mirrors Phase 3 lazy-require regression test); module-load of skills/content-review/scripts/index.js does NOT trigger require of skills/annotate/scripts/index.js (CRV-11)."
    - "scripts/cli.js: `node skills/content-review/scripts/cli.js <pptx> [--out <dir>] [--run-id <id>] [--annotate]` parses argv, calls runContentReview, prints jsonPath + mdPath + narrativePath; exits 0 on success, non-zero on validator throw with the validator's error message verbatim."
    - "render-content-fixed.js exports a pure function `render(findingsDoc) -> string` producing deterministic Markdown with five sections: §1 Deck-Level Argument Structure (Pyramid/MECE/narrative systemic findings, slideNum:null), §2 Inferred Argument Architecture (thesis + supports + tension/resolution beats), §3 Slide-by-Slide Content Findings, §4 Content Maturity Scoreboard, §5 Top 10 Content Fixes (D-04 / Phase 3 D-06 mirror)."
    - "Maturity scoreboard rubric (first-match-wins): 5 Persuasive (0 Critical AND ≤2 Major AND thesis in first 3 slides) / 4 Argued (0 Critical AND ≤4 Major) / 3 Informational (0 Critical AND ≥5 Major) / 2 Draft (1-2 Critical) / 1 Notes (≥3 Critical OR no thesis OR no resolution); deterministic — same findings doc → byte-identical output."
    - "render-content-fixed.js determinism invariants: no Date.now / Math.random / fs / async; sorts within each tier by `text` ascending (Phase 3 invariant carried forward)."
    - "skills/content-review/SKILL.md FULL BODY lands per the 10-item plugin-dev conformance checklist (§'SKILL.md plugin-dev Conformance' of 06-RESEARCH.md); description front-loads activation phrases per D-06 ('content review', 'argument quality', 'story flow', 'is my deck persuasive', 'Pyramid Principle / MECE', 'narrative arc'); body documents the 4 prompt-side checks (CRV-02 Pyramid/MECE, CRV-03 narrative-arc, CRV-05 claim/evidence, CRV-08 standalone-readability) using the prompt templates verbatim from §'The Seven (actually Eight) Content Checks' of 06-RESEARCH.md; documents the agent's hybrid orchestration flow (extract → run code-side libs → run prompt-side checks → merge → call runContentReview)."
    - "SKILL.md body includes the locked invariant verbatim: 'If you catch yourself writing about color, font, alignment, or layout, DELETE the line — that is /review's domain' (Pitfall 3 / CLAUDE.md content-vs-design boundary; CRV-10 prep)."
    - "SKILL.md frontmatter `user-invocable` flips false → true (D-07); name matches directory `content-review`; allowed-tools scoped per Phase 7 DIST-04 expectations (Bash(node:*), Bash(npm:*) — pptx-to-images.sh + soffice/pdftoppm only loaded transitively if --annotate)."
    - "references/content-checks.md provides the narrative reference doc the SKILL.md prompt links to for the 8 checks (consumed by agent, not by code)."
    - "tests/content-review-runtime.test.js: end-to-end with pre-built findings fixture asserts JSON + MD + narrativePath returned; sibling-of-input + run-dir-mirror both written; validator throws propagate (no swallow)."
    - "tests/content-review-render-fixed.test.js: same findingsDoc rendered twice → byte-identical; section ordering 1→5 stable; maturity scoreboard rubric exercised on each tier (5 fixtures, one per tier)."
    - "tests/content-review-lazy-annotate.test.js: `require('skills/content-review/scripts')` does NOT load skills/annotate/scripts/index.js (assert via require.cache inspection or a stubbed module-loader counter — same pattern as Phase 3 lazy-require test)."
  artifacts:
    - path: "skills/content-review/scripts/index.js"
      provides: "runContentReview orchestrator (CRV-09, CRV-11)"
      contains: "runContentReview"
    - path: "skills/content-review/scripts/cli.js"
      provides: "Standalone CLI entry point"
      contains: "process.argv"
    - path: "skills/content-review/scripts/render-content-fixed.js"
      provides: "Deterministic 5-section fixed-template Markdown renderer (D-04)"
      contains: "Content Maturity Scoreboard"
    - path: "skills/content-review/SKILL.md"
      provides: "Full body + user-invocable:true; 4 prompt-side checks; activation phrases (D-06/D-07; CRV-01/02/03/05/08)"
      contains: "user-invocable: true"
    - path: "skills/content-review/references/content-checks.md"
      provides: "Narrative reference for the 8 content checks"
      contains: "Pyramid"
  key_links:
    - from: "skills/content-review/scripts/index.js"
      to: "skills/review/scripts/lib/schema-validator.js"
      via: "validate(findings) before any IO"
      pattern: "validate"
    - from: "skills/content-review/scripts/index.js"
      to: "skills/annotate/scripts/index.js"
      via: "lazy require inside `if (annotate)` only — P-07"
      pattern: "require\\(.*annotate"
    - from: "skills/content-review/SKILL.md"
      to: "skills/content-review/scripts/index.js"
      via: "agent invokes runContentReview after assembling findings doc"
      pattern: "runContentReview"
    - from: "skills/content-review/scripts/render-content-fixed.js"
      to: "skills/content-review/scripts/index.js"
      via: "renderer called with full findings doc; pure function"
      pattern: "render\\("
---

<objective>
Wave 2 (sequential after 06-01): ship the user-facing surface. `runContentReview` orchestrator + CLI + deterministic fixed-template renderer + the full SKILL.md body covering the four prompt-side checks (Pyramid/MECE, narrative-arc, claim/evidence, standalone-readability) and the hybrid agent-orchestration flow. Flips `user-invocable: false → true` (D-07). Produces the sibling-of-input two-report output (D-04) and lazy-requires `/annotate` only when gated (P-07; CRV-11).

Purpose: This is the plan that makes `/instadecks:content-review` a working slash skill. Plan 06-01 shipped the contract + deterministic primitives; Plan 06-02 wires them into a runnable producer that emits findings against the v1.1 schema and routes optionally into `/annotate`.

Output: 5 source files (index.js, cli.js, render-content-fixed.js, SKILL.md full body, content-checks.md) + 3 test files green; SKILL.md plugin-dev 10-item checklist passes; lazy-require invariant holds; sibling-of-input + run-dir-mirror outputs verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-instadecks-content-review/06-CONTEXT.md
@.planning/phases/06-instadecks-content-review/06-RESEARCH.md
@.planning/phases/06-instadecks-content-review/06-01-PLAN.md
@skills/review/scripts/index.js
@skills/review/scripts/render-fixed.js
@skills/review/SKILL.md
@skills/create/SKILL.md

<interfaces>
<!-- Phase 3 architectural analog — runContentReview mirrors this shape -->
From skills/review/scripts/index.js (VERIFIED):
```js
async function runReview({deckPath, runId, outDir, mode='standalone',
                          findings, annotate=false, slidesToReview=null} = {}) {
  if (!deckPath) throw new Error('runReview: deckPath required');
  if (!findings) throw new Error('runReview: findings required ...');
  validate(findings);
  // resolve outputs (sibling-of-input + run-dir mirror)
  // render fixed MD
  if (annotate) {
    const runAnnotate = require('../../annotate/scripts').runAnnotate; // P-07 lazy
    annotated = await runAnnotate({deckPath, findings, outDir, runId});
  }
  return {jsonPath, mdPath, narrativePath, runDir, runId, findingCounts, genuineCount, annotated};
}
module.exports = { runReview };
```

From Plan 06-01 (now on disk):
```js
// skills/review/scripts/lib/schema-validator.js — v1.1
const VALID_CHECK_IDS = new Set(['action-title','redundancy','jargon','length',
                                  'pyramid-mece','narrative-arc','claim-evidence',
                                  'standalone-readability']);
function validate(findings) { /* ... */ }
module.exports = { validate, CATEGORIES, VALID_CHECK_IDS };

// skills/content-review/scripts/lib/{extract-content,title-adapter,redundancy,jargon,length-check}.js
// All return findings[] honoring schema v1.1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runContentReview orchestrator + CLI + lazy-annotate guard</name>
  <files>skills/content-review/scripts/index.js, skills/content-review/scripts/cli.js, tests/content-review-runtime.test.js, tests/content-review-lazy-annotate.test.js</files>
  <behavior>
    runtime test:
      - runContentReview({deckPath, runId, outDir, findings}) writes <deck>.content-review.json + .md + .narrative.md path returned (file NOT written by orchestrator — agent-authored downstream)
      - Run-dir mirror created at .planning/instadecks/<runId>/
      - Validator throw propagates verbatim (e.g., missing check_id on content finding)
      - Missing deckPath → throws 'runContentReview: deckPath required'
      - Missing findings → throws 'runContentReview: findings required ...'
      - Returns {jsonPath, mdPath, narrativePath, runDir, runId, findingCounts, genuineCount, annotated}
      - annotated === null when annotate:false; resolves to runAnnotate result when annotate:true (mock the module in test)
    lazy-annotate test:
      - delete require.cache for both modules; require('skills/content-review/scripts'); assert require.cache has no key matching /annotate\/scripts\/index\.js$/ until annotate:true is invoked
    cli test (subset of runtime test):
      - spawn node cli.js <fixture.pptx> --out <tmpDir> exits 0; stdout contains the three output paths
  </behavior>
  <action>
    Copy structure of skills/review/scripts/index.js verbatim where applicable; rename `runReview` → `runContentReview`; output stems `.content-review.{json,md,narrative.md}` (not `.review.{...}`); call render-content-fixed.js (Task 2) for the MD; drop `slidesToReview` parameter (CRV-11 — no auto-refine integration in v1). Validate findings BEFORE any IO. Lazy-require runAnnotate inside `if (annotate)` ONLY — copy the Phase 3 P-07 pattern verbatim. Build cli.js as a thin argv parser (no external dep — use process.argv + simple flag loop). All three test files must be green.
  </action>
  <verify>
    <automated>npm test -- tests/content-review-runtime.test.js tests/content-review-lazy-annotate.test.js</automated>
  </verify>
  <done>runContentReview API mirror of runReview; sibling-of-input + run-dir-mirror outputs verified; lazy-require invariant holds (require.cache check); CLI smoke test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render-content-fixed.js deterministic 5-section renderer</name>
  <files>skills/content-review/scripts/render-content-fixed.js, tests/content-review-render-fixed.test.js</files>
  <behavior>
    - render(findingsDoc) -> string with sections in fixed order: §1 Deck-Level Argument Structure / §2 Inferred Argument Architecture / §3 Slide-by-Slide Content Findings / §4 Content Maturity Scoreboard / §5 Top 10 Content Fixes
    - Same findingsDoc → byte-identical output across runs (no Date.now / Math.random / fs / async)
    - Findings within each tier sorted by `text` ascending (Phase 3 invariant)
    - Maturity rubric exercised: 5 fixtures (Persuasive / Argued / Informational / Draft / Notes); first-match-wins
    - Top-10 selects highest-leverage by severity then by section §3 ordering
    - Renders both code-side findings (check_id ∈ {action-title,redundancy,jargon,length}) and prompt-side findings (check_id ∈ {pyramid-mece,narrative-arc,claim-evidence,standalone-readability})
    - Empty findings doc → valid output with "No content findings emitted" placeholder per section
  </behavior>
  <action>
    Copy structure of skills/review/scripts/render-fixed.js verbatim; specialize section headers per §"Render-content-fixed structure" of 06-RESEARCH.md. Implement maturity rubric as first-match-wins switch per the table. Sort within tiers using localeCompare on `text`. Pure function — no IO, no async, no clock. Must be runnable in isolation: `render(JSON.parse(fs.readFileSync(fixture)))` from a test.
  </action>
  <verify>
    <automated>npm test -- tests/content-review-render-fixed.test.js</automated>
  </verify>
  <done>Renderer green; determinism property-test passes (10 random reorderings of input findings → same output); maturity rubric covers all 5 tiers in tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SKILL.md full body + content-checks.md reference + user-invocable flip</name>
  <files>skills/content-review/SKILL.md, skills/content-review/references/content-checks.md</files>
  <behavior>
    plugin-dev 10-item conformance (mirror skills/review/SKILL.md + skills/create/SKILL.md):
      1. frontmatter name === 'content-review' (matches dir)
      2. user-invocable: true (FLIPPED from false; D-07)
      3. description ≤1024 chars, third-person, imperative voice, front-loads CRV-01/D-06 activation phrases verbatim ("content review", "argument quality", "story flow", "is my deck persuasive", "Pyramid Principle / MECE", "narrative arc")
      4. allowed-tools scoped: Bash(node:*), Bash(npm:*), Read, Write, Glob, Grep
      5. body documents 4 prompt-side checks with prompt templates from 06-RESEARCH.md verbatim (CRV-02/03/05/08)
      6. body includes Pitfall 3 / CLAUDE.md boundary invariant verbatim ("If you catch yourself writing about color, font, alignment, or layout, DELETE the line")
      7. body documents hybrid orchestration: agent extracts via lib/extract-content.js → runs 4 code-side checks → runs 4 prompt-side checks → merges → calls runContentReview
      8. body documents two-report output (D-04): fixed MD via runContentReview, narrative MD authored post-call by agent
      9. body documents --annotate flag pipeline (CRV-11; gated by flag OR natural-language intent per inherited Phase 3 D-03)
      10. references/content-checks.md exists with the 8-check narrative reference (consumed by agent prompt links)
  </behavior>
  <action>
    Author SKILL.md per the 10-item checklist verbatim. Use skills/review/SKILL.md as the structural template (third-person voice, imperative description, embedded examples, allowed-tools scope). FLIP `user-invocable: false` → `true` in frontmatter (D-07). Description string MUST contain all six D-06 activation anchors. Body sections must include the four prompt templates from §"Check 5/6/7/8" of 06-RESEARCH.md COPIED VERBATIM (these are the calibrated check specs). Include the locked-boundary disclaimer. content-checks.md is the narrative reference doc — short, links from SKILL.md, summarizes all 8 checks (4 code, 4 prompt) with one paragraph each. NO test for SKILL.md prose content beyond frontmatter validity (covered in Plan 06-04 integration).
  </action>
  <verify>
    <automated>node tools/validate-manifest.js && grep -q 'user-invocable: true' skills/content-review/SKILL.md && grep -q 'content review' skills/content-review/SKILL.md && grep -q 'argument quality' skills/content-review/SKILL.md && grep -q 'Pyramid Principle' skills/content-review/SKILL.md && grep -q 'narrative arc' skills/content-review/SKILL.md && grep -q 'DELETE the line' skills/content-review/SKILL.md</automated>
  </verify>
  <done>SKILL.md frontmatter valid; user-invocable flipped to true; all 6 D-06 activation anchors present; boundary invariant verbatim; 4 prompt-check templates embedded; content-checks.md reference committed; manifest validator passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-merged findings → runContentReview | Untrusted (agent-authored) JSON crosses validator gate before any IO |
| outDir / runId from CLI argv | Untrusted user input crosses into fs paths |
| --annotate flag → lazy require | Crossing into the annotate adapter; validator already ran |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | runContentReview output paths | mitigate | Resolve outDir + runId via path.resolve + assert under cwd or .planning/instadecks/; reject `..` segments (mirror Phase 3 hardening from Plan 03-03) |
| T-06-06 | Spoofing | findings doc category="content" without check_id | mitigate | schema-validator (06-01) throws before any IO; runContentReview MUST call validate FIRST |
| T-06-07 | Information Disclosure | speaker notes leakage in fixed MD | accept | Notes already user-authored; rendering them in §3 is the documented behavior; no PII filter in v1 |
| T-06-08 | Elevation of Privilege | --annotate path traversal via deckPath | mitigate | Reuse Phase 2 D-07 hardened pptx-to-images.sh argv quoting; lazy-require ensures path is only consumed downstream after validator |
</threat_model>

<verification>
- All 3 test files green
- `git diff skills/annotate/` shows no changes (binary-asset invariant)
- `tools/validate-manifest.js` passes
- `tools/lint-paths.sh` passes (no hardcoded absolute paths)
- Manual smoke: `node skills/content-review/scripts/cli.js tests/fixtures/v8-reference/sample.pptx` (any existing fixture) writes the three output files
</verification>

<success_criteria>
SC#1 (8 checks invocable as a slash skill, activation phrases in description) — covered for the 4 prompt-side checks; full activation rate ≥8/10 measured in Phase 7 DIST-02. SC#2 (same schema/severity → /annotate) — pipeline functional via lazy-require + Plan 06-01 adapter patch. SC#4 (standalone in v1, output pipes into /annotate) — fully covered. CRV-01/02/03/05/08/09/11 fully covered. CRV-10 deferred to Plan 06-03.
</success_criteria>

<output>
After completion, create `.planning/phases/06-instadecks-content-review/06-02-SUMMARY.md`
</output>

---
phase: 04-instadecks-create-scaffold
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - skills/create/references/cookbook.md
  - skills/create/references/cookbook/title.md
  - skills/create/references/cookbook/section.md
  - skills/create/references/cookbook/2col.md
  - skills/create/references/cookbook/comparison.md
  - skills/create/references/cookbook/data-chart.md
  - skills/create/references/cookbook/data-table.md
  - skills/create/references/cookbook/stat-callout.md
  - skills/create/references/cookbook/quote.md
  - skills/create/references/cookbook/closing.md
  - skills/create/references/design-ideas.md
  - skills/create/references/design-ideas.json
  - tests/create-cookbook-recipes.test.js
autonomous: true
requirements: [CRT-02, CRT-03, CRT-04, CRT-05, CRT-15]

must_haves:
  truths:
    - "skills/create/references/cookbook.md exists as master index — links all 9 sub-recipe files, includes setup boilerplate (16:9 layout, palette tokens, addFooter helper) and DO/DON'T cheatsheet (CRT-02, CRT-04)."
    - "9 cookbook sub-recipes (title/section/2col/comparison/data-chart/data-table/stat-callout/quote/closing) each contain a JS code-fence with a callable render<Type>(slide, params) function using ONLY pres.shapes.* / pres.charts.* ENUMS — never string literals (CRT-15)."
    - "Each recipe includes the standard footer (page number + source line) and a slide.addNotes(...) call (CRT-05)."
    - "Each recipe includes a 'DO / DON'T' contrast example (action-title vs topic-title; ENUM vs string)."
    - "skills/create/references/design-ideas.md is AUTHOR-ORIGINAL prose (Q-1 license finding) — NOT verbatim Anthropic. Contains: 10 author-original palettes (with original names + original hex values), 8 typography pairings (IBM Plex Sans body anchor), 10 anti-patterns. Cites 'inspired by public design literature'."
    - "skills/create/references/design-ideas.json is the machine-readable companion: {palettes:[{name, primary, secondary, accent, ink, muted}], typography_pairs:[{heading, body, mono?}]} — every entry passes lib/design-validator.js (round-trip test)."
    - "tests/create-cookbook-recipes.test.js parses each recipe markdown, extracts the JS code-fence, runs it through lib/enum-lint.js (no string-literal addShape), and Function-instantiates the render function against a mock pres+slide to assert callable + uses ENUMS."
  artifacts:
    - path: "skills/create/references/cookbook.md"
      provides: "Master cookbook index + setup boilerplate (CRT-02)"
      contains: "LAYOUT_16x9"
    - path: "skills/create/references/cookbook/title.md"
      provides: "Recipe 1 — title slide"
      contains: "renderTitle"
    - path: "skills/create/references/cookbook/section.md"
      provides: "Recipe 2 — section divider"
      contains: "renderSection"
    - path: "skills/create/references/cookbook/2col.md"
      provides: "Recipe 3 — two-column"
      contains: "render2Col"
    - path: "skills/create/references/cookbook/comparison.md"
      provides: "Recipe 4 — comparison cards"
      contains: "renderComparison"
    - path: "skills/create/references/cookbook/data-chart.md"
      provides: "Recipe 5 — chart"
      contains: "renderDataChart"
    - path: "skills/create/references/cookbook/data-table.md"
      provides: "Recipe 6 — table"
      contains: "renderDataTable"
    - path: "skills/create/references/cookbook/stat-callout.md"
      provides: "Recipe 7 — stat callout"
      contains: "renderStatCallout"
    - path: "skills/create/references/cookbook/quote.md"
      provides: "Recipe 8 — quote"
      contains: "renderQuote"
    - path: "skills/create/references/cookbook/closing.md"
      provides: "Recipe 9 — closing (variant of title/section)"
      contains: "renderClosing"
    - path: "skills/create/references/design-ideas.md"
      provides: "Author-original design guidance (10 palettes / 8 typography / 10 anti-patterns) — Q-1 license-clean"
      contains: "inspired by"
    - path: "skills/create/references/design-ideas.json"
      provides: "Machine-readable pinned palettes + typography list (consumed by design-validator)"
      contains: "typography_pairs"
    - path: "tests/create-cookbook-recipes.test.js"
      provides: "Static lint + sandboxed parse-and-eval test for all 9 recipes + design-ideas.json round-trip"
      contains: "renderTitle"
  key_links:
    - from: "skills/create/references/design-ideas.json"
      to: "skills/create/scripts/lib/design-validator.js"
      via: "tests/create-cookbook-recipes.test.js round-trip — every palette+pair must pass validateDesignChoice"
      pattern: "validateDesignChoice"
    - from: "skills/create/references/cookbook/*.md"
      to: "skills/create/scripts/lib/enum-lint.js"
      via: "tests/create-cookbook-recipes.test.js extracts JS code-fence and runs lintCjs"
      pattern: "addShape\\s*\\(\\s*pres\\.shapes\\."
    - from: "skills/create/references/cookbook.md"
      to: "skills/create/references/cookbook/*.md"
      via: "Markdown links in master index table"
      pattern: "cookbook/.*\\.md"
---

<objective>
Ship the reference content layer of `/instadecks:create`: 9 per-recipe markdown files (one per slide type), a master cookbook index, and the **author-original** `design-ideas.md` + `design-ideas.json` (per Q-1 license finding — Anthropic's pptx skill is proprietary, NOT redistributable). Plus a static test that parse-and-evals every recipe against the lib/enum-lint guard and round-trips design-ideas.json through lib/design-validator.

Purpose: Wave 1 — the cookbook is the **reference** that the agent reads at runtime to compose `render-deck.cjs` per CRT-03 (per-run, NOT a fixed template). Recipes are copy-pasteable building blocks, not templates. Design-ideas is the author-original guidance the agent draws palette+typography from per D-04.

**Q-1 license note (CRITICAL):** Anthropic's bundled `pptx` skill `LICENSE.txt` forbids reproduction and derivative works. design-ideas.md is **author-original** — original palette names, original hex values, original prose — not "verbatim from Anthropic." NOTICE attribution (Plan 04-04) cites "inspired by" only.

Output: 11 reference markdown files + 1 JSON + 1 test file; ~12 test assertions; Plan 04-01's design-validator + enum-lint round-trip green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-instadecks-create-scaffold/04-CONTEXT.md
@.planning/phases/04-instadecks-create-scaffold/04-RESEARCH.md
@.planning/phases/04-instadecks-create-scaffold/04-PATTERNS.md

<interfaces>
**Recipe markdown structure (each cookbook/<type>.md follows):**
```markdown
# Recipe — <Type>

**Slide type:** <type>
**16:9:** 10″ × 5.625″
**Footer:** page number + source line
**Notes:** speaker notes via slide.addNotes()

## Code

\`\`\`javascript
function render<Type>(slide, { ...params }) {
  // body — uses pres.shapes.* / pres.charts.* ENUMS only
  // ends with addFooter(slide, {...}) + slide.addNotes(...)
}
\`\`\`

## DO / DON'T

| ✅ DO | ❌ DON'T |
|---|---|
| Action title: 'Q3 revenue grew 23%' | Topic title: 'Q3 Revenue' |
| `slide.addShape(pres.shapes.OVAL, …)` | `slide.addShape('oval', …)` |

## When to use

<1-2 sentences on which DeckBrief.narrative_arc beats this recipe fits>
```

The recipe code body is COPIED VERBATIM from `04-RESEARCH.md` §"The 9 Cookbook Recipes" lines 432–690. Do not invent — use the verified-against-pptxgenjs-4.0.1 snippets.

**Master `cookbook.md` shape:**
- Heading + 1-paragraph intro
- "Setup boilerplate (top of every render-deck.cjs)" — verbatim from 04-RESEARCH.md lines 396-430
- Index table linking all 9 sub-files with one-line descriptions
- "Global DO / DON'T cheatsheet" table (from 04-RESEARCH.md lines 695-704)

**`design-ideas.md` — AUTHOR-ORIGINAL (Q-1):**
- Heading + license-clarity note: "This guidance is original to Instadecks; the structural pattern (curated palettes + typography pairings + anti-patterns list) is inspired by public design-systems literature. No content here is copied or derived from any proprietary source."
- ## 10 Palettes — 10 entries with original names (NOT 'Midnight Executive', 'Forest & Moss', etc. — invent fresh names like 'Indigo Dawn', 'Slate Forest', 'Crimson Atelier', 'Plex Mono Noir', 'Verdant Steel', 'Saffron Brief', 'Glacier Ridge', 'Copper Field', 'Cobalt Plex', 'Cerulean Plex'). Each: `**<name>** — primary <hex>, secondary <hex>, accent <hex>, ink <hex>, muted <hex>. <2-sentence rationale: best-fit tone>.`
- ## 8 Typography Pairings — IBM Plex Sans is body anchor for at least 4 pairs. Pair shape: `**<heading-font>** + **<body-font>** — <when-to-use>.` Examples: `Inter + IBM Plex Sans` (executive); `IBM Plex Serif + IBM Plex Sans` (analytical); `Cambria + IBM Plex Sans` (corporate-traditional); `Calibri + IBM Plex Sans` (corporate-modern); `IBM Plex Sans + IBM Plex Sans` (mono-pair fallback); plus 3 more pairings.
- ## 10 Anti-Patterns — original prose covering: layout repetition (R18 anti-tell from Phase 3), body-text centering, insufficient size contrast, default-blue without justification, mixed paragraph spacing, partial styling, text-only slides, padding violations, low-contrast palettes, AI-tell accent lines under titles. Each: 2-3 sentence original prose. Tie back to design-validator rules where applicable.

**`design-ideas.json` (machine-readable companion):**
```json
{
  "palettes": [
    { "name": "Indigo Dawn", "primary": "1E2761", "secondary": "CADCFC", "accent": "FFFFFF", "ink": "0B1020", "muted": "6B7280" },
    ... 9 more, MUST match the 10 palette names + hexes in design-ideas.md
  ],
  "typography_pairs": [
    { "heading": "Inter", "body": "IBM Plex Sans" },
    { "heading": "IBM Plex Serif", "body": "IBM Plex Sans" },
    ... 6 more, matching the 8 pairings in design-ideas.md
  ]
}
```

**`tests/create-cookbook-recipes.test.js` shape:**
```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const { lintCjs } = require('../skills/create/scripts/lib/enum-lint');
const { validateDesignChoice } = require('../skills/create/scripts/lib/design-validator');

const RECIPES = ['title','section','2col','comparison','data-chart','data-table','stat-callout','quote','closing'];

function extractJsCodeFence(md) {
  const m = md.match(/```javascript\n([\s\S]*?)\n```/);
  if (!m) throw new Error('no JS code-fence');
  return m[1];
}

for (const recipe of RECIPES) {
  test(`recipe ${recipe}: code-fence parses + uses ENUM constants`, () => {
    const md = fs.readFileSync(path.join(__dirname, '..', 'skills', 'create', 'references', 'cookbook', `${recipe}.md`), 'utf8');
    const code = extractJsCodeFence(md);
    // Layer 2 enum-lint pass:
    lintCjs(code, { filename: `${recipe}.md` });
    // Sandboxed callable check: wrap in factory + call with mock pres/slide
    const factory = new Function('pres', `${code}\n; return render${recipeFnName(recipe)};`);
    const mockPres = { shapes: new Proxy({}, { get: () => 'ENUM_STUB' }), charts: new Proxy({}, { get: () => 'CHART_STUB' }) };
    const fn = factory(mockPres);
    assert.equal(typeof fn, 'function');
  });
}

test('design-ideas.json round-trips through validateDesignChoice', () => {
  const di = JSON.parse(fs.readFileSync(path.join(__dirname, '..', 'skills', 'create', 'references', 'design-ideas.json'), 'utf8'));
  const sampleBrief = JSON.parse(fs.readFileSync(path.join(__dirname, 'fixtures', 'sample-brief.json'), 'utf8'));
  for (const palette of di.palettes) {
    for (const typography of di.typography_pairs) {
      const r = validateDesignChoice({ palette, typography, brief: sampleBrief, designIdeas: di });
      assert.equal(r.ok, true, `palette=${palette.name} pair=${typography.heading}+${typography.body}: ${JSON.stringify(r.violations)}`);
    }
  }
});

test('every recipe md has DO/DON\'T table + addNotes call', () => {
  for (const recipe of RECIPES) {
    const md = fs.readFileSync(path.join(__dirname, '..', 'skills', 'create', 'references', 'cookbook', `${recipe}.md`), 'utf8');
    assert.match(md, /## DO \/ DON'T/);
    assert.match(md, /addNotes/);
  }
});
```

`recipeFnName('2col') === '2Col'` etc. — implement small helper for the recipe-to-function-name mapping (title→Title, section→Section, 2col→2Col, comparison→Comparison, data-chart→DataChart, data-table→DataTable, stat-callout→StatCallout, quote→Quote, closing→Closing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 9 cookbook recipes + master index</name>
  <files>skills/create/references/cookbook.md, skills/create/references/cookbook/title.md, skills/create/references/cookbook/section.md, skills/create/references/cookbook/2col.md, skills/create/references/cookbook/comparison.md, skills/create/references/cookbook/data-chart.md, skills/create/references/cookbook/data-table.md, skills/create/references/cookbook/stat-callout.md, skills/create/references/cookbook/quote.md, skills/create/references/cookbook/closing.md</files>
  <read_first>
    - /Users/shafqat/Documents/Projects/instadecks/.planning/phases/04-instadecks-create-scaffold/04-RESEARCH.md (§"The 9 Cookbook Recipes" lines 392-704 — verbatim source for each recipe code body + DO/DON'T cheatsheet)
    - /Users/shafqat/Documents/Projects/instadecks/skills/review/references/findings-schema.md (style precedent for reference docs — frontmatter, headings)
  </read_first>
  <action>
    **Step A — author master `cookbook.md`:**
    Write `/Users/shafqat/Documents/Projects/instadecks/skills/create/references/cookbook.md` with:
    1. H1 + 1-paragraph intro: "This cookbook is the reference Claude reads at runtime to compose a per-run `render-deck.cjs` for /instadecks:create. Each recipe is a copy-pasteable building block — never a template. Per CRT-03, render-deck.cjs is authored fresh per run, content-tuned to the DeckBrief."
    2. "Locked invariants" callout: ENUM-only shapes (CRT-15); no `#` hex; fresh option objects; action titles (D-06).
    3. ## Setup boilerplate — verbatim from `04-RESEARCH.md` lines 396-430 (the `'use strict'; const pptxgen = require('pptxgenjs'); ... function addFooter(...)` block).
    4. ## Recipe index — table:
       | # | Slide type | Recipe file | When to use |
       |---|---|---|---|
       | 1 | title | [title.md](cookbook/title.md) | Opening + topic statement |
       | 2 | section | [section.md](cookbook/section.md) | Major narrative breaks |
       | 3 | 2-column | [2col.md](cookbook/2col.md) | Side-by-side bullets |
       | 4 | comparison | [comparison.md](cookbook/comparison.md) | Option-A vs Option-B cards |
       | 5 | data-chart | [data-chart.md](cookbook/data-chart.md) | Quantitative trend |
       | 6 | data-table | [data-table.md](cookbook/data-table.md) | Multi-row tabular detail |
       | 7 | stat-callout | [stat-callout.md](cookbook/stat-callout.md) | Hero statistic |
       | 8 | quote | [quote.md](cookbook/quote.md) | Attributed quote |
       | 9 | closing | [closing.md](cookbook/closing.md) | CTA / wrap (variant of title) |
    5. ## Global DO / DON'T cheatsheet — verbatim table from RESEARCH lines 695-704.
    6. ## See also — link to `design-ideas.md` and `design-ideas.json`.

    **Step B — author 9 recipe files:**
    For each recipe in RECIPES, write `/Users/shafqat/Documents/Projects/instadecks/skills/create/references/cookbook/<recipe>.md` per the <interfaces> structure. The JS code body is verbatim from `04-RESEARCH.md`:
    - title.md → lines 432-456
    - section.md → lines 458-479
    - 2col.md → lines 481-517
    - comparison.md → lines 519-554
    - data-chart.md → lines 556-580
    - data-table.md → lines 582-611
    - stat-callout.md → lines 613-639
    - quote.md → lines 641-663
    - closing.md → lines 665-691

    Each recipe file must include:
    - H1 `# Recipe — <Type>`
    - "Slide type / 16:9 / Footer / Notes" metadata block
    - ## Code — fenced ```javascript ... ``` block (the verbatim render function)
    - ## DO / DON'T — at least 2 contrast rows (action-title vs topic-title; ENUM vs string; plus one recipe-specific row e.g. `chartType` ENUM key)
    - ## When to use — 1-2 sentences mapping to DeckBrief.narrative_arc beats

    **Step C — verify:**
    ```bash
    ls skills/create/references/cookbook.md skills/create/references/cookbook/*.md | wc -l   # expect 10
    grep -c 'pres.shapes\.' skills/create/references/cookbook/*.md   # each recipe that uses shapes uses ENUM
    bash tools/lint-paths.sh
    ```

    **Step D — atomic commit:**
    ```bash
    git add skills/create/references/cookbook.md skills/create/references/cookbook/*.md
    git commit -m "$(cat <<'EOF'
    docs(04-02): add cookbook master index + 9 per-slide-type recipes

    All 9 recipes use pres.shapes.* / pres.charts.* ENUMS only (CRT-15);
    each carries a DO/DON'T contrast (ENUM vs string; action-title vs topic);
    every recipe ends with addFooter() + slide.addNotes() (CRT-05).
    Code bodies are verbatim from 04-RESEARCH.md §"The 9 Cookbook Recipes",
    verified against pptxgenjs 4.0.1 type definitions.

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>test -f skills/create/references/cookbook.md && for r in title section 2col comparison data-chart data-table stat-callout quote closing; do test -f "skills/create/references/cookbook/$r.md" || { echo "missing $r"; exit 1; }; done && ! grep -RE "addShape\s*\(\s*['\"]" skills/create/references/cookbook/ && bash tools/lint-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - 10 reference files exist (1 master + 9 recipes).
    - No string-literal `addShape('...')` anywhere in cookbook tree.
    - Each recipe contains: H1, code-fence, DO/DON'T table, addNotes() call, addFooter() call (except title where convention is no-footer per RESEARCH line 451).
    - Master cookbook.md links all 9 sub-files.
    - lint-paths.sh green.
    - Atomic commit `docs(04-02): ...`.
  </acceptance_criteria>
  <done>9 recipe files + master index ship; reference layer ready for agent runtime composition.</done>
</task>

<task type="auto">
  <name>Task 2: Author-original design-ideas.md + design-ideas.json + cookbook test</name>
  <files>skills/create/references/design-ideas.md, skills/create/references/design-ideas.json, tests/create-cookbook-recipes.test.js</files>
  <read_first>
    - /Users/shafqat/Documents/Projects/instadecks/.planning/phases/04-instadecks-create-scaffold/04-RESEARCH.md (§"Q-1 — Anthropic pptx Skill License Findings" lines 353-388 — author-original constraint)
    - /Users/shafqat/Documents/Projects/instadecks/skills/create/scripts/lib/design-validator.js (just-shipped — for round-trip test)
    - /Users/shafqat/Documents/Projects/instadecks/skills/create/scripts/lib/enum-lint.js (just-shipped — for code-fence lint)
    - /Users/shafqat/Documents/Projects/instadecks/tests/fixtures/sample-brief.json (just-shipped — for round-trip)
  </read_first>
  <action>
    **Step A — author `design-ideas.md` (AUTHOR-ORIGINAL per Q-1):**
    Write `/Users/shafqat/Documents/Projects/instadecks/skills/create/references/design-ideas.md`. **CRITICAL:** Do NOT copy from any external source. All palette names, hex values, font-pair rationale, and anti-pattern prose must be original.

    Structure:
    ```markdown
    # Design Ideas — Instadecks

    > This guidance is original to Instadecks. The structural pattern (curated palettes
    > + typography pairings + anti-patterns) is inspired by public design-systems
    > literature. No content here is copied or derived from any proprietary source.

    ## How to use

    When composing render-deck.cjs, pick:
    1. One **palette** matching `DeckBrief.tone` (executive → cooler/darker; analytical → balanced ink+muted; playful → warmer/saturated).
    2. One **typography pairing** — IBM Plex Sans is bundled (SIL OFL) and serves as the body anchor for most pairs.
    3. Avoid the 10 **anti-patterns** below — `lib/design-validator.js` enforces a subset (default-blue, off-list typography, malformed hex).

    ## 10 Palettes

    Each palette: primary / secondary / accent / ink (body text) / muted (chart axes, footers).

    **Indigo Dawn** — primary `1E2761`, secondary `CADCFC`, accent `FFFFFF`, ink `0B1020`, muted `6B7280`. Best for executive narratives wanting depth without corporate-blue cliché.

    **Slate Forest** — primary `2F3E2E`, secondary `B7C9A8`, accent `F4F1E8`, ink `1A1F18`, muted `5E6B58`. Calm, considered; suits analytical decks with environmental themes.

    **Crimson Atelier** — primary `8B1E3F`, secondary `F2CBC4`, accent `FBF5EC`, ink `2A0E15`, muted `8A6F77`. Confident, editorial; good for brand-positioning decks.

    **Plex Mono Noir** — primary `0B0B0F`, secondary `D4D4D8`, accent `FAFAFA`, ink `09090B`, muted `52525B`. High-contrast monochrome; ideal for engineering technical decks.

    **Verdant Steel** — primary `1F4D40`, secondary `B5D8C9`, accent `F5F8F6`, ink `0F1F1A`, muted `5A6F69`. Trustworthy, grounded; works for healthcare/scientific topics.

    **Saffron Brief** — primary `B8651A`, secondary `F4D9B5`, accent `FFFAF0`, ink `2C1810`, muted `8B7355`. Warm and direct; pairs with playful or storytelling tones.

    **Glacier Ridge** — primary `1E5A7A`, secondary `B8DCE8`, accent `F4FAFC`, ink `0F1E2A`, muted `5C7689`. Cool and analytical without defaulting to corporate blue.

    **Copper Field** — primary `9C4221`, secondary `EBC8B8`, accent `FFF8F1`, ink `2D1410`, muted `8B6A5C`. Earthy, distinctive; good for product launches.

    **Cobalt Plex** — primary `2546B0`, secondary `BFD0F0`, accent `F5F8FF`, ink `0B1837`, muted `5A6B8C`. Saturated cobalt — explicitly NOT default-PowerPoint-blue; signals deliberate selection.

    **Cerulean Plex** — primary `0E7490`, secondary `B5E2EB`, accent `F0FAFB`, ink `082F38`, muted `4A6C73`. Crisp, modern; fits SaaS or data-product decks.

    ## 8 Typography Pairings

    IBM Plex Sans (bundled, SIL OFL) is the body anchor.

    1. **Inter + IBM Plex Sans** — executive, high-density information.
    2. **IBM Plex Serif + IBM Plex Sans** — analytical, long-form claims.
    3. **Cambria + IBM Plex Sans** — corporate-traditional (Cambria ships with most MS installs; substitution-safe).
    4. **Calibri + IBM Plex Sans** — corporate-modern (Calibri ships with MS installs).
    5. **IBM Plex Sans + IBM Plex Sans** — single-family fallback when target platform may not have alternates.
    6. **IBM Plex Mono + IBM Plex Sans** — engineering, technical, code-heavy decks.
    7. **Georgia + IBM Plex Sans** — editorial, narrative, storytelling.
    8. **Helvetica Neue + IBM Plex Sans** — minimalist, design-forward (Mac-native; falls back to Arial on Win).

    ## 10 Anti-Patterns

    1. **Layout repetition.** Re-using the same recipe across more than 3 consecutive slides creates the AI-authored signature `/instadecks:review` flags as R18. Vary recipe across narrative-arc beats.

    2. **Body-text centering.** Center-aligning body bullets reduces scanability. Left-align body content; reserve center alignment for hero stats and titles on title/closing slides.

    3. **Insufficient size contrast.** When heading and body sizes differ by less than ~1.6×, the visual hierarchy collapses. Headings ≥ 24pt; body 12-14pt; stat-callout headlines 60-72pt.

    4. **Default PowerPoint blue without justification.** `0070C0`, `1F4E79`, `2E75B6` and their close cousins are the unmistakable default-template fingerprint. `lib/design-validator.js` flags these unless `DeckBrief.tone` or `topic` references "corporate" / "blue" / "finance" explicitly.

    5. **Mixed paragraph spacing.** Switching between `paraSpaceAfter` and `lineSpacing` mid-deck produces gappy, inconsistent rhythm. Pick one; cookbook recipes default to `paraSpaceAfter: 6` for bullets.

    6. **Partial styling.** Bolding only the first half of a list, or italicizing one slide's quotes but not another's, signals carelessness. Apply emphasis rules consistently across the deck.

    7. **Text-only slides past slide 3.** A wall-of-bullets slide deep in the deck is reader fatigue. Break up with stat-callouts, quotes, or charts.

    8. **Padding violations.** Shapes flush against slide edges (x < 0.4 or x+w > 9.6) read as cropped. Cookbook recipes use `MARGIN_X = 0.5`.

    9. **Low-contrast palettes.** WCAG AA requires ≥ 4.5:1 for body text. Avoid muted-on-accent or accent-on-secondary combinations for body content.

    10. **Accent lines under titles (R18 AI-tell).** A horizontal divider directly below the slide title is the LLM-authored signature `/instadecks:review` flags. If a divider is needed, place it at section-rail position (e.g., under the section number, see `cookbook/section.md`) — never under a title.
    ```

    **Step B — author `design-ideas.json`:**
    Machine-readable companion. MUST contain the same 10 palette names + hex values + the same 8 typography pairings, in the JSON shape from <interfaces>. Validate JSON syntax: `node -e "JSON.parse(require('fs').readFileSync('skills/create/references/design-ideas.json'))"`.

    **Step C — author `tests/create-cookbook-recipes.test.js`:**
    Body per <interfaces>. Helper `recipeFnName` maps slug → PascalCase function name. Three test groups:
    1. Per-recipe: parse markdown → extract JS code-fence → run lintCjs → Function-instantiate render fn → assert callable.
    2. design-ideas.json round-trip: every (palette × typography_pair) cross-product passes validateDesignChoice with sample-brief.json.
    3. Cookbook hygiene: every recipe md contains `## DO / DON'T` and `addNotes`.

    **Step D — verify:**
    ```bash
    node -e "JSON.parse(require('fs').readFileSync('skills/create/references/design-ideas.json'))"   # JSON syntax
    node --test tests/create-cookbook-recipes.test.js
    bash tools/lint-paths.sh
    # Author-original sniff: ensure no Anthropic palette names
    ! grep -E "Midnight Executive|Forest & Moss" skills/create/references/design-ideas.md
    ```

    **Step E — atomic commit:**
    ```bash
    git add skills/create/references/design-ideas.md skills/create/references/design-ideas.json tests/create-cookbook-recipes.test.js
    git commit -m "$(cat <<'EOF'
    docs(04-02): add author-original design-ideas.md/json + cookbook test

    Per Q-1 license finding (Anthropic pptx skill is proprietary; cannot bundle
    verbatim), design-ideas.md is original prose with original palette names +
    original hex values. NOTICE attribution (Plan 04-04) cites "inspired by
    public design literature" only — no derivative-works claim.

    tests/create-cookbook-recipes.test.js asserts:
    - every recipe code-fence passes lib/enum-lint.js (CRT-15);
    - every (palette × typography_pair) round-trips through lib/design-validator.js;
    - every recipe md carries DO/DON'T table + addNotes call (CRT-05).

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('skills/create/references/design-ideas.json'))" && node --test tests/create-cookbook-recipes.test.js && bash tools/lint-paths.sh && ! grep -qE "Midnight Executive|Forest & Moss" skills/create/references/design-ideas.md</automated>
  </verify>
  <acceptance_criteria>
    - design-ideas.md has 10 palettes + 8 typography pairs + 10 anti-patterns, all author-original.
    - design-ideas.json parses; palette count + pair count match the markdown.
    - tests/create-cookbook-recipes.test.js green (all 9 recipes pass enum-lint + factory; round-trip green; hygiene green).
    - No Anthropic-skill palette names present (sniff grep returns no match).
    - lint-paths.sh green.
    - Atomic commit `docs(04-02): ...`.
  </acceptance_criteria>
  <done>Reference content layer ships license-clean; cookbook+design-ideas validated by Plan 04-01's libs.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Repudiation | Bundling Anthropic prose verbatim → Apache-2.0 license violation (Q-1) | mitigate | Author-original design-ideas.md (original names, hexes, prose); NOTICE in Plan 04-04 cites "inspired by" only; sniff-grep in CI prevents Anthropic-name regression. |
| T-04-06 | Tampering | Recipe md drifts and silently introduces string-literal addShape | mitigate | tests/create-cookbook-recipes.test.js extracts code-fence + runs lib/enum-lint.js on every recipe on every test run. |
| T-04-07 | Information Disclosure | design-ideas.json palette/pair drifts out of sync with .md | mitigate | Round-trip test asserts every JSON entry validates; manual audit in Plan 04-04 final check. |
</threat_model>

<verification>
- `ls skills/create/references/cookbook.md skills/create/references/cookbook/*.md | wc -l` → 10.
- `node --test tests/create-cookbook-recipes.test.js` green.
- No string-literal addShape in `skills/create/references/cookbook/`.
- design-ideas.json parses + round-trips through validateDesignChoice.
- Sniff-grep for Anthropic palette names returns no match.
- 2 atomic commits.
</verification>

<success_criteria>
- 9 cookbook recipes + master index + author-original design-ideas (md + json) shipped.
- All recipes use ENUM constants only (CRT-15).
- All recipes include footer + speaker notes (CRT-05).
- 8+ slide types covered for CRT-04 (closing is variant; cookbook ships 9 recipes total).
- design-ideas.json round-trips through Plan 04-01's design-validator (CRT-02 deterministic guardrail).
- Q-1 license finding honored: zero verbatim Anthropic content.
</success_criteria>

<output>
`.planning/phases/04-instadecks-create-scaffold/04-02-SUMMARY.md` — file inventory (10 markdown + 1 JSON + 1 test), license-compliance note (author-original per Q-1), recipe-by-recipe table mapping → cookbook slug → render-fn-name → ENUM constants used, downstream consumers (Plan 04-03 enum-lint CI gate scans this tree; Plan 04-04 SKILL.md tells the agent to read this layer at runtime).
</output>

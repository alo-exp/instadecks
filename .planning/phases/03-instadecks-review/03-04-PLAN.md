---
phase: 03-instadecks-review
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - skills/review/scripts/render-fixed.js
  - tests/fixtures/sample-findings.fixed.md
  - tests/review-render-fixed.test.js
autonomous: true
requirements: [RVW-02, RVW-05]

must_haves:
  truths:
    - "skills/review/scripts/render-fixed.js exports render(findingsDoc) → string. Pure function: no fs, no async, no LLM, no clock — given the same input, produces byte-identical output."
    - "Output structure follows DECK-VDA §1/§2/§3/§4/§5 ordering per RESEARCH §'Two-Report Architecture' (locked D-06 layout)."
    - "§4 maturity rubric implements first-matching-rule logic per RESEARCH §'Maturity rubric' (any 🔴 or 🟠>5 → Draft; 0🔴 with 3≤🟠≤5 → Internal-ready; 0🔴, 🟠≤2 → Client-ready; 0🔴/0🟠/🟡≤10 → Partner-ready)."
    - "§5 produces a top-10 fixes table ranked by severity then frequency-of-fix-text (deterministic tie-break)."
    - "Snapshot test compares render(canonical fixture) byte-for-byte against tests/fixtures/sample-findings.fixed.md — any drift fails the test."
    - "render-fixed.js REPLACES the Plan 03-02 stub (which prints 'render-fixed.js stub' warning); the stub's banner instructed Plan 03-04 to overwrite it."
  artifacts:
    - path: "skills/review/scripts/render-fixed.js"
      provides: "Deterministic DECK-VDA fixed-template Markdown renderer"
      contains: "Top 10 Highest-Leverage Fixes"
    - path: "tests/fixtures/sample-findings.fixed.md"
      provides: "Snapshot baseline for render(sample-findings.json)"
    - path: "tests/review-render-fixed.test.js"
      provides: "RVW-02 snapshot + property tests"
      contains: "render-fixed"
  key_links:
    - from: "tests/review-render-fixed.test.js"
      to: "skills/review/scripts/render-fixed.js + tests/fixtures/sample-findings.json + tests/fixtures/sample-findings.fixed.md"
      via: "render(sampleFindings) === fs.readFileSync(snapshotPath)"
      pattern: "snapshot"
    - from: "skills/review/scripts/render-fixed.js"
      to: "skills/review/references/findings-schema.md (severity / category enums)"
      via: "consumes findings array; emoji map keyed off severity_reviewer 4-tier"
      pattern: "severity_reviewer"
---

<objective>
Ship the deterministic DECK-VDA fixed-template Markdown renderer (`skills/review/scripts/render-fixed.js`) replacing the Plan 03-02 stub. Pure function: same input → byte-identical output. Snapshot-tested against `tests/fixtures/sample-findings.fixed.md`. Implements RVW-02 (DECK-VDA §1/§3/§4/§5 structure) and the §RVW-05 fixed-template emission obligation.

Purpose: The auditable, regression-testable counterpart to the LLM-authored narrative MD (D-06 dual emission). Phase 5 auto-refine consumes the JSON; humans read the narrative; the fixed MD is the audit trail.
Output: A pure-function renderer + locked snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@silver-bullet.md
@.planning/phases/03-instadecks-review/03-CONTEXT.md
@.planning/phases/03-instadecks-review/03-RESEARCH.md
@skills/review/references/findings-schema.md
@tests/fixtures/sample-findings.json

<interfaces>
**Public surface:**
```js
// skills/review/scripts/render-fixed.js
function render(findingsDoc) // → string (byte-deterministic Markdown)
module.exports = { render, _internal: { renderSection1, renderSection2, renderSection3, renderSection4, renderSection5, computeMaturity, severityEmoji } };
```

**Output skeleton (RESEARCH §"Fixed-template renderer"):**

```markdown
# Design Review — {deck filename}

> Generated {findingsDoc.generated_at} · {N} slides reviewed · maturity: **{Draft|Internal-ready|Client-ready|Partner-ready}**

## §1 — Deck-Level Systemic Findings

(loop findings where slideNum === null OR location === 'deck-systemic')
- **{emoji} {Severity}** | {Category} — {text}
  - *Standard:* {standard}
  - *Fix:* {fix}
(if no §1 findings: "_No deck-level systemic findings._")

## §2 — Inferred Design System

_See SKILL.md §2 — agent declares baseline before §3 (narrative MD captures this)._

## §3 — Slide-by-Slide Findings

### Slide {N} — {title}

Findings: 🔴×{c} / 🟠×{m} / 🟡×{i} / ⚪×{n}

🔴 CRITICAL
- {text} — {standard} → {fix}

🟠 MAJOR
- ...

🟡 MINOR
- ...

⚪ NITPICK
- ...

(repeat per slide; tier sections omitted entirely if empty for that slide)

## §4 — Summary Scoreboard

| Metric | Count |
|--------|-------|
| Total slides audited | {N} |
| Critical (🔴) | {total_critical} |
| Major (🟠) | {total_major} |
| Minor (🟡) | {total_minor} |
| Nitpick (⚪) | {total_nitpick} |
| Slides with ≥1 Critical | {count} |
| Genuine findings | {count} |
| **Overall design maturity** | **{maturity}** |

## §5 — Top 10 Highest-Leverage Fixes

| # | Fix | Affected slides | Severity resolved | Effort |
|---|-----|-----------------|-------------------|--------|
| 1 | {fix-text} | {S1, S2, …} | {Critical/Major/Minor/Nitpick} | trivial |
```

**Determinism rules:**
- NO `Date.now()` — only `findingsDoc.generated_at` is rendered.
- NO `Math.random()`.
- Sort orders: §1 by `(severity_index, text)`; §3 slides by `slideNum` ascending; within slide by `(severity_index, text)`; §5 by `(severity_index, frequency_desc, fix_text)` for stable tie-break.
- Severity index: Critical=0, Major=1, Minor=2, Nitpick=3.
- Effort heuristic for §5: trivial if `text.length < 60`; light if 60-120; moderate if 120-200; substantial otherwise. (Coarse but deterministic.)
- §5 fix grouping: collapse findings sharing identical `fix` text into a single row with merged "Affected slides" column.

**Maturity rubric (verbatim, first matching wins):**

```js
function computeMaturity(counts) {
  if (counts.critical > 0 || counts.major > 5) return 'Draft';
  if (counts.major >= 3 && counts.major <= 5) return 'Internal-ready';
  if (counts.major <= 2 && counts.minor + counts.nitpick > 10) return 'Client-ready';
  if (counts.major <= 2) return 'Client-ready';
  if (counts.major === 0 && counts.minor <= 10) return 'Partner-ready';
  return 'Draft';
}
```

(Per RESEARCH §"Maturity rubric" — first matching rule wins; preserve `>` for major and `≤` for the other comparators.)

**Severity emoji map:**

```js
const SEVERITY_EMOJI = { Critical: '🔴', Major: '🟠', Minor: '🟡', Nitpick: '⚪' };
```

Locked — these are the DECK-VDA glyphs from RESEARCH §"4-tier severity".

**Snapshot generation strategy:**
1. Implement render() in Step A.
2. Run once: pipe `render(sample-findings.json)` to `tests/fixtures/sample-findings.fixed.md`.
3. Inspect output manually; confirm §1-§5 structure, maturity rubric correct, §5 ≤10 entries.
4. Commit the snapshot.
5. Future render() changes that alter output for the canonical fixture force a deliberate snapshot update + commit message documenting why.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement render-fixed.js + generate snapshot</name>
  <files>skills/review/scripts/render-fixed.js, tests/fixtures/sample-findings.fixed.md</files>
  <read_first>
    - /Users/shafqat/Documents/Projects/instadecks/.planning/phases/03-instadecks-review/03-RESEARCH.md (§"Fixed-template renderer"; §"Maturity rubric"; §"Section ordering")
    - /Users/shafqat/Documents/Projects/instadecks/skills/review/references/findings-schema.md (severity / category enums)
    - /Users/shafqat/Documents/Projects/instadecks/tests/fixtures/sample-findings.json (canonical fixture — input)
    - /Users/shafqat/Documents/Projects/instadecks/skills/review/scripts/render-fixed.js (Plan 03-02 stub — REPLACE entirely)
  </read_first>
  <action>
    **Step A — implement render-fixed.js:** Replace the Plan 03-02 stub entirely. Include banner header citing D-06 + RVW-02/05.

    Implementation guide:
    1. Constants: `SEVERITY_EMOJI`, `SEVERITY_INDEX = {Critical:0, Major:1, Minor:2, Nitpick:3}`.
    2. Helpers: `severityEmoji(s)`, `sortFindings(arr)` (by severity_index then text), `groupBySlide(doc)`, `countBySeverity(doc)`, `countSlidesWithCritical(doc)`, `countGenuine(doc)`.
    3. `renderSection1(doc)`: filter findings where `slideNum === null || location === 'deck-systemic'`; render bullets with emoji + standard + fix. Empty case → `_No deck-level systemic findings._`.
    4. `renderSection2()`: static text per skeleton.
    5. `renderSection3(doc)`: per-slide block; render only non-empty severity tiers.
    6. `renderSection4(doc, counts, maturity)`: pipe table with metrics.
    7. `renderSection5(doc)`: collapse findings by `fix` text; sort by `(severity_index, -frequency, fix)`; cap at 10; effort heuristic per text length.
    8. `computeMaturity(counts)`: per the locked rubric above (verbatim).
    9. `render(doc)`: glue with `# Design Review — ${doc.deck}` H1 + blockquote line `> Generated ${doc.generated_at} · ${doc.slides.length} slides reviewed · maturity: **${maturity}**` + sections joined by `\n\n`.

    **Step B — generate snapshot:**

    Run a one-shot Node command that calls `render()` against `tests/fixtures/sample-findings.json` and writes the output to `tests/fixtures/sample-findings.fixed.md`. Open the snapshot and SANITY CHECK by hand:
    - H1 contains the deck name from the fixture.
    - §1, §2, §3, §4, §5 sections all present.
    - §3 contains every slide from the fixture in numeric order.
    - §4 maturity = correct value per the rubric (verify by hand-counting the fixture's findings).
    - §5 has ≤10 rows.

    **Step C — verify (regenerate must produce same bytes):**

    ```bash
    node -e "const r=require('./skills/review/scripts/render-fixed').render(JSON.parse(require('fs').readFileSync('tests/fixtures/sample-findings.json'))); const s=require('fs').readFileSync('tests/fixtures/sample-findings.fixed.md','utf8'); if(r!==s){process.exit(1)}"
    bash tools/lint-paths.sh
    ```

    **Step D — atomic commit:**

    ```
    git add skills/review/scripts/render-fixed.js tests/fixtures/sample-findings.fixed.md
    git commit -m "$(cat <<'EOF'
    feat(03-04): replace render-fixed.js stub with DECK-VDA fixed-template renderer

    Pure function: render(findingsDoc) → byte-deterministic Markdown.
    Implements §1/§2/§3/§4/§5 ordering per D-06; maturity rubric per locked
    first-matching-rule logic; §5 collapses findings by fix-text and caps at 10.
    Snapshot tests/fixtures/sample-findings.fixed.md committed as the locked
    baseline — any future render() drift fails review-render-fixed.test.js
    until the snapshot is intentionally regenerated.

    Covers RVW-02 deterministic side; RVW-05 fixed-MD emission obligation.

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>node -e "const r=require('./skills/review/scripts/render-fixed').render(JSON.parse(require('fs').readFileSync('tests/fixtures/sample-findings.json'))); const s=require('fs').readFileSync('tests/fixtures/sample-findings.fixed.md','utf8'); if(r!==s)process.exit(1)" &amp;&amp; bash tools/lint-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `render-fixed.js` exports `render` and `_internal` block; banner identifies it as the Plan 03-04 replacement of the Plan 03-02 stub.
    - Calling render() on the canonical fixture produces output containing all five sections (§1, §2, §3, §4, §5) and a maturity word.
    - Snapshot file `tests/fixtures/sample-findings.fixed.md` exists and matches the live render() output byte-for-byte.
    - `bash tools/lint-paths.sh` green.
    - Atomic commit `feat(03-04): ...`.
  </acceptance_criteria>
  <done>Renderer ships; snapshot locked; downstream test (Task 2) asserts byte-stability.</done>
</task>

<task type="auto">
  <name>Task 2: tests/review-render-fixed.test.js — snapshot + property tests</name>
  <files>tests/review-render-fixed.test.js</files>
  <read_first>
    - /Users/shafqat/Documents/Projects/instadecks/skills/review/scripts/render-fixed.js (just shipped)
    - /Users/shafqat/Documents/Projects/instadecks/tests/fixtures/sample-findings.json (input)
    - /Users/shafqat/Documents/Projects/instadecks/tests/fixtures/sample-findings.fixed.md (snapshot)
    - /Users/shafqat/Documents/Projects/instadecks/.planning/phases/02-instadecks-annotate/02-PATTERNS.md (test banner pattern)
  </read_first>
  <action>
    Create `tests/review-render-fixed.test.js` with banner header citing RVW-02/05 and D-06. Subtests:

    1. **Snapshot test (locked baseline):**
       - `const actual = render(JSON.parse(fs.readFileSync(SAMPLE_PATH, 'utf8')));`
       - `const expected = fs.readFileSync(SNAPSHOT_PATH, 'utf8');`
       - `assert.strictEqual(actual, expected, 'render-fixed drift — regenerate sample-findings.fixed.md only with deliberate plan-checker review');`

    2. **Determinism property:**
       - Call `render()` 5 times in a loop on the canonical fixture; assert all 5 outputs are byte-identical.

    3. **Maturity rubric — Critical bumps to Draft:**
       - Construct synthetic doc with 0 majors but 1 critical → `assert(/maturity: \*\*Draft\*\*/.test(render(doc)))`.

    4. **Maturity rubric — clean deck → Partner-ready:**
       - Synthetic doc with 0/0/0/2 → `assert(/maturity: \*\*Partner-ready\*\*/.test(render(doc)))`.

    5. **Maturity rubric — exactly 4 majors → Internal-ready:**
       - 0/4/0/0 → `Internal-ready`.

    6. **§5 caps at 10 rows:**
       - Construct synthetic doc with 25 distinct fix-texts; assert §5 table has at most 10 data rows (count by `^| \d+ |` regex).

    7. **No clock leak:**
       - Wrap render() execution between two `Date.now()` reads; assert second-by-second drift does NOT change the output (compare two renders separated by `setTimeout 1100ms`).

    8. **Severity-preservation in output:**
       - Output includes the four glyphs `🔴`, `🟠`, `🟡`, `⚪` only when those tiers have findings; never the 3-tier collapsed labels `MAJOR`, `MINOR`, `POLISH` outside the per-slide block headers.

    Verify:
    ```bash
    node --test tests/review-render-fixed.test.js
    find tests -maxdepth 2 -name '*.test.js' -print0 | xargs -0 node --test
    ```

    Atomic commit:
    ```
    git add tests/review-render-fixed.test.js
    git commit -m "$(cat <<'EOF'
    test(03-04): add snapshot + property tests for render-fixed.js

    Locks tests/fixtures/sample-findings.fixed.md as the regression baseline
    (any drift fails the snapshot subtest). Property tests cover determinism,
    the locked maturity rubric (4 cases), §5 row cap, no-clock-leak, and
    severity-preservation in the rendered glyphs.

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>node --test tests/review-render-fixed.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All 8 subtests pass.
    - Snapshot subtest fails loudly with the documented drift message if `render()` output changes.
    - `find tests -maxdepth 2 -name '*.test.js' -print0 | xargs -0 node --test` green.
    - Atomic commit `test(03-04): ...`.
    - **Drift runbook:** when render-fixed.js is intentionally modified post-Plan 03-04, the developer regenerates the snapshot via the Plan 03-04 Step B command, sanity-checks the diff against the prior snapshot, and includes the `tests/fixtures/sample-findings.fixed.md` file in the same commit as the render-fixed.js change. Plan-checker confirms the diff is intentional.
  </acceptance_criteria>
  <done>Renderer locked behind snapshot + 7 property assertions; future drift caught immediately.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering | render() leaking wall-clock or random into output | mitigate | "No clock leak" subtest asserts byte-identical output across 1.1s gap; pure-function discipline enforced by code review. |
| T-03-16 | DoS | Pathologically large findings doc → very long render | accept | Bounded by upstream `runReview` 10MB cap on the JSON file (Phase 2 adapter precedent + Plan 03-02 inheritance). |
| T-03-17 | Information Disclosure | Markdown output containing absolute paths from `deck` field | accept | `findings.deck` is whatever the producer wrote; sibling-of-input convention gives basenames in normal flow. |
</threat_model>

<verification>
- `node --test tests/review-render-fixed.test.js` green (8 subtests).
- `find tests -maxdepth 2 -name '*.test.js' -print0 | xargs -0 node --test` green.
- `bash tools/lint-paths.sh` green.
- `node tools/assert-pptxgenjs-pin.js` green.
- Two atomic commits.
</verification>

<success_criteria>
- RVW-02 satisfied (DECK-VDA §1/§3/§4/§5 deterministic structure).
- RVW-05 fixed-MD emission obligation closed.
- Plan 03-02 stub fully replaced; integration tests in 03-02 still pass against the real renderer (re-run as part of `find tests` full suite).
</success_criteria>

<output>
`.planning/phases/03-instadecks-review/03-04-SUMMARY.md` — snapshot byte size, all 4 maturity-rubric branches exercised, drift-runbook reaffirmation.
</output>

---
context: phase
phase: 01-plugin-foundation-contract-ci-gates
status: phase_complete
last_updated: 2026-04-28T02:16:12.128Z
next_phase: 02-instadecks-annotate
---

# Phase 1 Complete — Resume at Phase 2

Phase 1 (Plugin Foundation, Contract & CI Gates) is fully closed:
- 9 plans executed across 4 waves
- 30+ atomic commits on `main`
- All Silver Bullet enforcement gates satisfied (review-context ×2, review-research ×2, plan-checker ×2 consecutive clean, code-reviewer ×2 consecutive clean, gsd-verifier ×2 consecutive clean)
- VERIFICATION.md status: `passed` (6/6 success criteria; SC-1 fresh-clone smoke verified autonomously via `/tmp/instadecks-fresh-smoke-$$` clone-and-run-all-gates)
- 41 unpushed commits on local `main` (push deferred — requires user approval per shared-state rules)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Skipping SB enforcement under "autonomous decision" rationalization | Earlier in this session I proposed skipping the formal `/artifact-reviewer review-research` dispatch and a second verification pass in the name of context budget. The user course-corrected: "Actually, do NOT try to skip any Silver Bullet enforcement." Both back-filled. | blocking | (a) Always dispatch formal artifact-reviewer skills when silver-bullet §3a-i / WFIN-* mandates them — inline self-review is not a substitute. (b) Always run the full 2-consecutive-clean count for plan-checker / code-reviewer / verifier / security-auditor (silver-bullet §3a EXRV-01..04); if pass 1 had issues, pass 2 + pass 3 are needed. (c) When a verifier returns `human_needed`, route via AskUserQuestion per execute-phase workflow — never auto-accept inline. (d) Treat context budget as a reason to dispatch subagents (fresh contexts), not as a reason to compress steps. (e) Memory at `/Users/shafqat/.claude/projects/-Users-shafqat-Documents-Projects-instadecks/memory/feedback_silver_bullet_strict.md` captures this — read at session start. |

## Required Reading (in order)

1. `silver-bullet.md` — enforcement rules (overrides defaults; §3a review loops, §3a-i post-command review gates WFIN-04..07, §3 NON-NEGOTIABLE rule on "no minimization due to context concerns")
2. `CLAUDE.md` — locked invariants + project-specific rules
3. `.planning/PROJECT.md` — milestone v0.1.0 context
4. `.planning/REQUIREMENTS.md` — Phase 2 requirements are ANNO-01..ANNO-11
5. `.planning/ROADMAP.md` §"## Phase 2" — phase goal + success criteria
6. `.planning/HANDOFF.json` — machine-readable session state
7. `.planning/phases/01-plugin-foundation-contract-ci-gates/01-VERIFICATION.md` — what's been delivered
8. `.planning/research/SUMMARY.md` — alignment decisions affecting Phase 2 (severity collapse, annotate.js verbatim, schema mapping)
9. `/Users/shafqat/Documents/Projects/Sourcevo/v5-blue-prestige/annotate.js` — the source-of-truth file Phase 2 copies verbatim

## Phase 1 Deliverables (use as foundation for Phase 2)

- `.claude-plugin/plugin.json` — plugin manifest
- `hooks/check-deps.sh`, `hooks/hooks.json` — SessionStart non-blocking hook with sentinel-gated `npm ci`
- `skills/{annotate,review,content-review,create}/SKILL.md` — skill skeletons (Phase 2+ owns full content)
- `skills/review/references/findings-schema.md` — locked v1.0 JSON contract; full 4-tier severity at producer side
- `tests/fixtures/sample-findings.json` — canonical fixture exercising all 4 severity tiers + 3 categories + `genuine: false`
- `tests/fixtures/v8-reference/` — `samples.js` (extracted from v8 SAMPLES), `Annotations_Sample.pptx` + `.sha256`, `annotate.js.sha256` (PRE-PATCH — Phase 2 replaces), 3× per-slide PNGs at 150 dpi
- `tests/annotate-integrity.test.js` — `it.skip` stub; **Phase 2 unsuspends after copying file + recording post-patch SHA**
- `tools/validate-manifest.js`, `tools/lint-paths.sh`, `tools/assert-pptxgenjs-pin.js` — CI gates
- `assets/fonts/IBM_Plex_Sans/` — 4 TTFs (Regular/Bold/Italic/BoldItalic) + OFL.txt + README; auto-install on macOS/Linux via `fc-cache`
- `LICENSE` (Apache-2.0 + bundled-software section), `NOTICE` (with relicensing note for annotate.js), `licenses/{IBM_Plex_Sans,image-size,jszip,pptxgenjs}/LICENSE`
- `package.json` (pptxgenjs `4.0.1` exact), `package-lock.json` (committed)
- `.github/workflows/ci.yml` — wires manifest validator, path lint, version-pin assert, license-checker, full test suite via `find tests -maxdepth 2 -name '*.test.js' -print0 | xargs -0 node --test` (glob — auto-discovers Phase 2+ tests)
- `README.md` — install + skill skeleton + license

## Locked Conventions Phase 2+ Inherits

- Test runner: `node --test` (zero-dep, Node 18+)
- Commit format: HEREDOC with `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>`
- Commit subject prefix: `<type>(<phase>-<plan>): <message>` e.g. `feat(02-01): ...`
- Atomic per-task commits; one SUMMARY.md per plan
- All paths via `${CLAUDE_PLUGIN_ROOT}` / `${CLAUDE_PLUGIN_DATA}` — `tools/lint-paths.sh` enforces
- pptxgenjs `4.0.1` exact pin — `tools/assert-pptxgenjs-pin.js` enforces
- Skill descriptions: single-line, ≤1024 chars, start with imperative verb — `tools/validate-manifest.js` enforces
- Lint allowlist comment: `# lint-allow:hardcoded-path` (allows targeted exceptions)
- License-checker fails on `GPL;AGPL;SSPL`; allows MIT/Apache-2.0/BSD-2/BSD-3/ISC/OFL-1.1

## Infrastructure State

- `node_modules/` populated locally (gitignored); fresh-clone install proven working via `npm ci --omit=dev` (19 packages, 0 vulnerabilities)
- IBM Plex Sans installed locally (18 weights detected via `fc-list`)
- soffice 26.2.2.2 + pdftoppm 26.02.0 + node v25.6.0 all present
- Worktree from Plan 03 already cleaned up

## Phase 2 Goal & Approach

**Phase 2 goal (from ROADMAP):** A standalone-invocable `/instadecks:annotate` skill that consumes findings JSON in the locked schema and produces annotated PPTX + PDF overlays with byte-identical (or pixel-diff < 0.5%) parity to v8 BluePrestige reference output.

**Phase 2 requirement IDs:** ANNO-01..ANNO-11.

**Critical Phase 2 work:**
1. Copy `/Users/shafqat/Documents/Projects/Sourcevo/v5-blue-prestige/annotate.js` to `skills/annotate/scripts/annotate.js` VERBATIM with `// VERBATIM v8 BLUE PRESTIGE — DO NOT EDIT` banner.
2. Apply the documented one-line require-path patch so pptxgenjs resolves out of `${CLAUDE_PLUGIN_DATA}/node_modules` instead of `../node_modules`. THIS IS THE ONLY PERMITTED MODIFICATION.
3. Compute post-patch SHA-256, replace `tests/fixtures/v8-reference/annotate.js.sha256` with the new value (banner: `POST-PATCH (require-path patched)`).
4. Unsuspend `tests/annotate-integrity.test.js` (remove `test.skip` / `it.skip`).
5. Extract `SAMPLES` from `annotate.js` to a separate `samples.js` module (so geometry stays unmodified); runtime data is written into a fresh JSON consumed via the same module shape.
6. Build the JSON-to-SAMPLES adapter implementing 4→3 severity collapse (Critical→`major`, Major→`major`, Minor→`minor`, Nitpick→`polish`) AT THE ADAPTER ONLY. annotate.js continues to see only `'major' | 'minor' | 'polish'`.
7. Adapter filters `genuine == true` findings.
8. Slide-image symlink approach: temp working dir, symlink `slide-NN.jpg` → `v8s-NN.jpg` to satisfy annotate.js's hardcoded image-name expectation without code changes.
9. Output: annotated PPTX + annotated PDF, written to a run directory + project-relative output path.
10. Standalone-invocable mode + pipelined invocation mode (in-memory deck-spec handoff for `/review`).
11. Validate against `tests/fixtures/sample-findings.json` → byte-identical PPTX vs v8 reference (or pixel-diff < 0.5%).

**Highest-risk subsystem in v0.1.0** — approach with `/gsd-discuss-phase 2` first to surface adapter boundary questions (pipelined vs standalone, error handling, output path conventions, soffice race-condition handling deferral to Phase 3) before planning.

## Pre-Execution Critique Required

Not applicable — Phase 1 is complete and all artifacts are committed. Phase 2 starts fresh from `/gsd-discuss-phase 2`.

## Next Action

Resume command:
```
/gsd-discuss-phase 2
```

Or to view full session state:
```
/gsd-resume-work
```

## Pending Human Action (non-blocking)

- **Push 41 commits to `alo-exp/instadecks` origin/main.** Local `main` is ahead by 41 commits since `git log origin/main..HEAD --oneline | wc -l` = 41 at session pause. Pushing requires user approval per shared-state rules in CLAUDE.md and silver-bullet.md §7. Recommended before Phase 2 kicks off so other contributors / CI infrastructure can see Phase 1 state, but not strictly required to begin Phase 2 locally.

---
phase: 07-marketplace-release
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - LICENSE
  - NOTICE
  - licenses/pptxgenjs/LICENSE
  - licenses/jszip/LICENSE
  - licenses/image-size/LICENSE
  - licenses/IBM_Plex_Sans/LICENSE
  - tools/license-audit.js
  - tests/license-audit.test.js
  - README.md
  - package.json
autonomous: true
requirements: [DIST-04, DIST-05, DIST-07]
must_haves:
  truths:
    - "license-checker (already a devDep) reports zero GPL/AGPL transitive deps in production tree (D-04, SC#3)"
    - "Every bundled dep listed in NOTICE has a corresponding licenses/<dep>/LICENSE file (D-04, SC#3)"
    - "LICENSE bundled-software section enumerates the same set as NOTICE — no drift (D-04)"
    - "README.md has Install / Quick Start / 4 Skills table / /instadecks:doctor self-check / Architecture (1 paragraph) / License / Contributing / Acknowledgements sections (D-07, SC#5)"
    - "README.md has the badge row (build status, version 0.1.0, license Apache-2.0) (D-07)"
    - "tools/license-audit.js fails CI if NOTICE deps and licenses/ dir drift apart, OR if license-checker reports any GPL/AGPL transitive (D-04)"
  artifacts:
    - path: "tools/license-audit.js"
      provides: "Single CI script that runs license-checker + drift-check between NOTICE and licenses/ dir"
      exports: ["run"]
    - path: "tests/license-audit.test.js"
      provides: "node:test green/red coverage of license-audit.js"
    - path: "README.md"
      provides: "Finalized v0.1.0 README per D-07"
      contains: "/instadecks:doctor"
    - path: "LICENSE"
      provides: "Apache-2.0 + bundled-software section reconciled with NOTICE"
      contains: "BUNDLED SOFTWARE"
    - path: "NOTICE"
      provides: "Per-bundled-dep attribution + DECK-VDA + design-ideas inspired-by — final audit pass"
      contains: "annotate.js"
    - path: "licenses/pptxgenjs/LICENSE"
      provides: "Verbatim MIT license text from upstream pptxgenjs 4.0.1"
    - path: "licenses/jszip/LICENSE"
      provides: "Verbatim MIT license text from upstream jszip"
    - path: "licenses/image-size/LICENSE"
      provides: "Verbatim MIT license text from upstream image-size"
    - path: "licenses/IBM_Plex_Sans/LICENSE"
      provides: "Verbatim SIL OFL 1.1 text from upstream IBM Plex"
  key_links:
    - from: "tools/license-audit.js"
      to: "node_modules/license-checker"
      via: "require('license-checker').init()"
      pattern: "license-checker"
    - from: "tools/license-audit.js"
      to: "NOTICE + licenses/"
      via: "filesystem comparison"
      pattern: "licenses/[a-zA-Z0-9_-]+/LICENSE"
    - from: "README.md"
      to: "skills/doctor/SKILL.md"
      via: "documentation link to /instadecks:doctor self-check"
      pattern: "/instadecks:doctor"
    - from: "package.json"
      to: "tools/license-audit.js"
      via: "npm script"
      pattern: "audit:licenses"
---

<objective>
Wave 2 of Phase 7 — license-compliance final pass and README finalization. Depends on Plan 07-01 only because the README references the doctor skill landed there.

Purpose: SC#3 (license compliance final) and SC#5 (README + doctor self-check). DIST-04 (license-checker zero-GPL), DIST-05 (per-dep licenses/ dir), DIST-07 (README finalized).

Output: license-audit.js CI gate, completed licenses/ directory (4 deps), reconciled NOTICE + LICENSE bundled-software section, finalized README with badges + skills table + quick-start + doctor self-check section. Atomic commits per artifact group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-marketplace-release/07-CONTEXT.md
@.planning/phases/07-marketplace-release/07-01-PLAN.md
@.planning/ROADMAP.md
@CLAUDE.md
@LICENSE
@NOTICE
@README.md
@package.json
@.claude-plugin/plugin.json

<interfaces>
<!-- Existing license-checker devDep is already pinned in package.json -->
<!-- license-checker programmatic API: -->
```js
const checker = require('license-checker');
checker.init({ start: process.cwd(), production: true }, (err, packages) => {
  // packages: { "name@ver": { licenses, repository, publisher, ... } }
});
```

<!-- Existing licenses/ dir already has 4 subdirs but contents not audited: -->
- licenses/pptxgenjs/   (verify LICENSE present and matches upstream MIT)
- licenses/jszip/       (verify LICENSE present and matches upstream MIT)
- licenses/image-size/  (verify LICENSE present and matches upstream MIT)
- licenses/IBM_Plex_Sans/ (verify LICENSE present and matches upstream SIL OFL 1.1)

<!-- NOTICE current bundled deps (must mirror licenses/ dir): -->
- pptxgenjs (MIT)
- IBM Plex Sans (SIL OFL 1.1)
- jszip (MIT or GPL-3.0; used under MIT)
- image-size (MIT)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools/license-audit.js + tests + licenses/ dir audit</name>
  <files>tools/license-audit.js, tests/license-audit.test.js, licenses/pptxgenjs/LICENSE, licenses/jszip/LICENSE, licenses/image-size/LICENSE, licenses/IBM_Plex_Sans/LICENSE, package.json</files>
  <behavior>
    - GREEN: license-checker reports zero GPL/AGPL on production deps; every NOTICE-listed bundled dep has a non-empty licenses/<dep>/LICENSE file → exit 0
    - RED: A GPL/AGPL transitive in production tree → exit 1 with `{package, license}` row
    - RED: NOTICE lists a dep with no licenses/<dep>/LICENSE — exit 1 with drift report
    - RED: licenses/ has a dir with no NOTICE entry — exit 1 with drift report
    - audit-script accepts whitelist for known-multi-license deps (jszip is "MIT OR GPL-3.0"; we use under MIT — explicit allow-row in the script)
  </behavior>
  <action>
    Create `tools/license-audit.js`:
    - `require('license-checker').init({ start: process.cwd(), production: true, excludePackages: '' })` — programmatic call.
    - For each package result: classify license string. Forbidden = anything matching `/\bGPL\b/i` or `/\bAGPL\b/i` UNLESS the package name is in an explicit whitelist (`jszip` is "(MIT OR GPL-3.0)" and we use under MIT — whitelist entry: `{ name: 'jszip', license_used: 'MIT', rationale: 'jszip is MIT OR GPL-3.0; we use under MIT (NOTICE)' }`).
    - Drift check:
      - Parse NOTICE: regex `/^- (\S+) \((.+?)\)/m` — extract `[name, license]` rows from the BUNDLED SOFTWARE ATTRIBUTION block.
      - Parse `licenses/` directory: list subdirs.
      - Assert NOTICE-name set === licenses-subdir set (modulo a trivial slug map — `IBM Plex Sans` → `IBM_Plex_Sans`).
    - `run() -> { ok, violations: [] }`. CLI entry exits 1 on `!ok`.

    Create `tests/license-audit.test.js`:
    - Mock license-checker output via dependency-injection (factor `runCheck(pkgs, noticeText, licensesDir)` as a pure inner fn so tests don't need to spawn license-checker). Top-level `run()` wires the real license-checker.
    - 5 test cases: green / GPL transitive (no whitelist) / GPL transitive (with whitelist) / NOTICE dep missing licenses/ entry / licenses/ dir without NOTICE entry.

    Audit `licenses/<dep>/LICENSE` files — each must exist and contain the upstream verbatim text:
    - `licenses/pptxgenjs/LICENSE` — MIT text from `node_modules/pptxgenjs/LICENSE.txt` (or LICENSE — whichever upstream ships).
    - `licenses/jszip/LICENSE` — MIT text from `node_modules/jszip/LICENSE.markdown` (jszip ships dual MIT/GPL — embed the MIT clause as the operative license; reference the upstream dual notice).
    - `licenses/image-size/LICENSE` — MIT text from `node_modules/image-size/LICENSE`.
    - `licenses/IBM_Plex_Sans/LICENSE` — SIL OFL 1.1 from the bundled IBM Plex archive (already shipped under assets/fonts; copy the OFL.txt verbatim).

    For each licenses file: if it already exists with the correct upstream content, leave alone; if missing or empty, copy verbatim from node_modules (or assets/fonts archive for IBM Plex).

    Patch `package.json`:
    - Add `"audit:licenses": "node tools/license-audit.js"` to scripts.

    Atomic commits:
    - `feat(07-02): add tools/license-audit.js + tests for GPL gate + NOTICE/licenses drift per D-04`
    - `chore(07-02): backfill licenses/<dep>/LICENSE verbatim from upstream per D-04` (only if any file was added or changed)
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/instadecks &amp;&amp; node --test tests/license-audit.test.js &amp;&amp; node tools/license-audit.js</automated>
  </verify>
  <done>
    - All test cases pass (mocked + real)
    - `node tools/license-audit.js` against the live tree exits 0
    - All 4 licenses/<dep>/LICENSE files exist and are non-empty
    - jszip whitelist entry present + rationale documented in script comments
  </done>
</task>

<task type="auto">
  <name>Task 2: NOTICE + LICENSE bundled-software final pass</name>
  <files>NOTICE, LICENSE</files>
  <action>
    Reconcile NOTICE and the LICENSE bundled-software section so they enumerate identical sets — script's drift-check (Task 1) enforces this on every CI run.

    NOTICE final state (per D-04):
    - Header: copyright line + Apache-2.0 grant (already present — no change).
    - RELICENSING NOTE for annotate.js (already present — no change).
    - BUNDLED SOFTWARE ATTRIBUTION block: 4 entries (pptxgenjs, IBM Plex Sans, jszip, image-size) with format `- <name> (<license>) — <upstream URL>` and a Copyright sub-line. Verify all 4 present with full upstream URLs.
    - DECK-VDA methodology attribution paragraph (already present — review wording).
    - Author-original design-ideas attribution paragraph (already present — review wording).
    - R18 AI-tell pattern attribution (already present — review wording).
    - Add NEW paragraph (if not already present) at end: "## annotate.js binary-asset note — `skills/annotate/scripts/annotate.js` is bundled VERBATIM from internal Sourcevo v8 BluePrestige (see CLAUDE.md SHA-pin invariant); only documented modification is the require-path patch."

    LICENSE bundled-software section (already present at line 204+):
    - Verify the 4 dep lines mirror NOTICE exactly:
      - `- pptxgenjs (MIT) — https://github.com/gitbrent/PptxGenJS`
      - `- IBM Plex Sans (SIL OFL 1.1) — https://github.com/IBM/plex`
      - `- jszip (MIT or GPL-3.0; used under MIT) — transitive of pptxgenjs`
      - `- image-size (MIT) — transitive of pptxgenjs`
    - If any drift exists between LICENSE and NOTICE, fix LICENSE to match NOTICE (NOTICE is the source of truth — it has the more complete attribution).

    Run `node tools/license-audit.js` — must exit 0 (drift-check passes).

    Atomic commit: `docs(07-02): reconcile NOTICE + LICENSE bundled-software final pass per D-04`
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/instadecks &amp;&amp; node tools/license-audit.js &amp;&amp; grep -c "pptxgenjs\|IBM Plex Sans\|jszip\|image-size" NOTICE | awk '$1>=4{exit 0} {exit 1}' &amp;&amp; grep -c "pptxgenjs\|IBM Plex Sans\|jszip\|image-size" LICENSE | awk '$1>=4{exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - NOTICE has all 4 bundled-dep entries + 3 attribution paragraphs (DECK-VDA, design-ideas, R18) + annotate.js binary-asset note
    - LICENSE bundled-software section mirrors NOTICE
    - license-audit.js drift-check exits 0
    - No regressions to RELICENSING NOTE or copyright header
  </done>
</task>

<task type="auto">
  <name>Task 3: README.md finalization with badges + skills table + doctor self-check + architecture</name>
  <files>README.md</files>
  <action>
    Rewrite README.md per D-07 / SC#5. Sections required (in this order):

    1. **H1 + tagline** — `# Instadecks` / "Generate, review, and annotate polished presentation decks from any input."
    2. **Badge row** — markdown badge syntax for:
       - Build status (placeholder: `![CI](https://github.com/alo-exp/instadecks/actions/workflows/ci.yml/badge.svg)` — even if CI workflow not yet active; tag-time provides the URL)
       - Version: `![Version](https://img.shields.io/badge/version-0.1.0-blue)`
       - License: `![License](https://img.shields.io/badge/license-Apache--2.0-green)`
    3. **Overview** — preserve current 2-paragraph block (pipeline composition narrative).
    4. **Install** — preserve marketplace install + local-dev clone steps; add a 3rd step under Install: "After install, run `/instadecks:doctor` to self-check prerequisites" with a link to the doctor skill.
    5. **Quick Start** — NEW section. 4 minimal one-liners — one per user-invocable skill — showing the canonical natural-language invocation (matched to activation-panel exemplar prompts):
       ```
       /instadecks:create — "Build me a pitch deck from this brief: ..."
       /instadecks:review — "Review my deck for design defects: deck.pptx"
       /instadecks:content-review — "Is my deck persuasive? deck.pptx"
       /instadecks:annotate — "Overlay these findings on my deck: deck.pptx findings.json"
       ```
    6. **Skills** — preserve the 4-row table; ADD a 5th row for `/instadecks:doctor` with description "Self-check Instadecks system prerequisites (soffice, pdftoppm, node, pptxgenjs, IBM Plex Sans) and report green/red with install hints."
    7. **`/instadecks:doctor` Self-Check** — NEW H2 section. 1 paragraph explaining purpose + per-prereq install hints (Mac brew + Linux apt + Windows choco):
       - LibreOffice / soffice
       - Poppler / pdftoppm
       - Node ≥ 18
    8. **Architecture** — NEW 1-paragraph H2. "Instadecks is a Claude Code marketplace plugin shipping 5 namespaced slash skills under `skills/`. The `/instadecks:annotate` skill bundles `annotate.js` verbatim from v8 BluePrestige (SHA-pinned binary asset) and consumes findings JSON in a locked v1.0/v1.1 schema. `/instadecks:review` and `/instadecks:content-review` produce findings in that schema; `/instadecks:create` composes a per-run `render-deck.cjs` from a curated pptxgenjs cookbook and wraps the output in an auto-refine convergence loop. `/instadecks:doctor` self-checks system prerequisites. Single repo, single npm tree, pinned `pptxgenjs@4.0.1`."
    9. **Requirements** — preserve current section.
    10. **License** — preserve current section.
    11. **Contributing** — NEW H2. Single paragraph: "Issues and PRs welcome at https://github.com/alo-exp/instadecks. Run `npm test` and `node tools/lint-paths.sh && node tools/audit-allowed-tools.js && node tools/license-audit.js` before opening a PR."
    12. **Acknowledgements** — NEW H2. Bullet list:
        - DECK-VDA methodology — Shafqat Ullah / Sourcevo (see NOTICE)
        - pptxgenjs by Brent Ely (MIT)
        - IBM Plex Sans by IBM (SIL OFL 1.1)
        - jszip by Stuart Knightley (MIT/GPL — used under MIT)
        - image-size by Aditya Yadav (MIT)

    Constraints:
    - DO NOT use words "v1", "v2", "future enhancement", "placeholder", "minimal" anywhere in README (CLAUDE.md scope-reduction prohibition).
    - Every skill-name reference uses the namespaced form `/instadecks:<name>` — not bare `/<name>`.
    - All paths absolute or relative-to-repo-root; no hardcoded user paths (lint-paths.sh enforces).

    Atomic commit: `docs(07-02): finalize README with badges + quick-start + doctor + architecture + acknowledgements per D-07`
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/instadecks &amp;&amp; bash tools/lint-paths.sh &amp;&amp; grep -q "## Quick Start" README.md &amp;&amp; grep -q "## /instadecks:doctor" README.md &amp;&amp; grep -q "## Architecture" README.md &amp;&amp; grep -q "## Contributing" README.md &amp;&amp; grep -q "## Acknowledgements" README.md &amp;&amp; grep -q "img.shields.io/badge/version-0.1.0" README.md &amp;&amp; ! grep -E "\\bv1\\b|\\bv2\\b|placeholder|future enhancement|minimal implementation" README.md</automated>
  </verify>
  <done>
    - All 12 sections present in correct order
    - Badge row renders 3 shields
    - 5-skill table including doctor
    - lint-paths.sh exits 0 (no hardcoded paths in README)
    - Scope-reduction sentinel grep finds zero matches
    - Quick-start examples mirror activation-panel exemplar phrasings (consistency between docs)
  </done>
</task>

</tasks>

<verification>
- `node --test tests/license-audit.test.js` — passes
- `node tools/license-audit.js` — exits 0
- `node tools/audit-allowed-tools.js` — still exits 0 (no regression from Plan 07-01)
- `bash tools/lint-paths.sh` — passes
- `grep -c "BUNDLED SOFTWARE" LICENSE && grep -c "BUNDLED SOFTWARE ATTRIBUTION" NOTICE` — both ≥ 1
- README has 12 ordered sections + 3 badges + 5-row skills table
- Three atomic commits land cleanly
</verification>

<success_criteria>
- DIST-04 / SC#3 license compliance final: zero GPL/AGPL prod deps, full licenses/ dir, NOTICE + LICENSE reconciled
- DIST-05 per-dep licenses/ dir audited and complete
- DIST-07 / SC#5 README finalized with badges, quick-start, /instadecks:doctor self-check, architecture, acknowledgements
- license-audit.js + audit-allowed-tools.js both green (compose into npm test or invoked from CI in Plan 07-03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-marketplace-release/07-02-SUMMARY.md` per the GSD summary template. Include:
- license-audit.js exit-0 evidence
- NOTICE + LICENSE reconciled diff summary
- README new sections list
- Atomic-commit SHAs
</output>
</content>
</invoke>
---
phase: 07-marketplace-release
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - tests/PERMISSION-MODE.md
  - tests/FRESH-INSTALL.md
  - tests/activation-results.md
  - .planning/RELEASE.md
  - .planning/marketplace-pr.md
  - .planning/marketplace-entry.json
autonomous: false
requirements: [DIST-01, DIST-02, DIST-03, DIST-06]
must_haves:
  truths:
    - "Activation panel scored ≥8/10 per skill across all 4 user-invocable skills (D-01, SC#1)"
    - "All 5 skills tested under `default` AND `dontAsk` permission modes — tool calls pre-approved or prompt cleanly per scoped allowed-tools (D-02, SC#2)"
    - "Fresh-machine install path validated: /plugin install → /instadecks:doctor green → /instadecks:create produces valid deck → /instadecks:review JSON → /instadecks:annotate overlay (D-06, SC#4)"
    - ".planning/RELEASE.md captures the v0.1.0 sign-off log: activation results, permission-mode results, fresh-install results, license-audit + audit-allowed-tools green, license-checker green (SC#1..#5)"
    - "Marketplace PR draft (.planning/marketplace-pr.md + .planning/marketplace-entry.json) ready to paste into alo-labs/claude-plugins (D-05, SC#4)"
    - "v0.1.0 git tag prepared (annotated, message references RELEASE.md) — actual `git tag` push gated on user approval at the final checkpoint"
  artifacts:
    - path: "tests/PERMISSION-MODE.md"
      provides: "Manual checklist: 5 skills × 2 modes (default / dontAsk) = 10 invocations + result table per D-02"
      contains: "default"
    - path: "tests/FRESH-INSTALL.md"
      provides: "6-step manual checklist per D-06 — Mac + Windows columns"
      contains: "/plugin install"
    - path: "tests/activation-results.md"
      provides: "Filled scoring matrix (4 skills × 10 prompts) with per-skill pass/fail at ≥8/10 gate"
      contains: "Pass"
    - path: ".planning/RELEASE.md"
      provides: "v0.1.0 sign-off log: SC#1..SC#5 evidence, audit-tool greens, manual-checklist greens, git tag command, marketplace-PR-link placeholder"
      contains: "v0.1.0"
    - path: ".planning/marketplace-pr.md"
      provides: "Draft PR body for alo-labs/claude-plugins describing the addition, testing evidence, and reviewer notes"
    - path: ".planning/marketplace-entry.json"
      provides: "Verbatim JSON entry to add to alo-labs/claude-plugins marketplace.json per D-05"
      contains: "instadecks"
  key_links:
    - from: ".planning/RELEASE.md"
      to: "tests/activation-results.md"
      via: "sign-off references the filled scoring matrix"
      pattern: "activation-results"
    - from: ".planning/RELEASE.md"
      to: "tests/PERMISSION-MODE.md"
      via: "sign-off references default + dontAsk run results"
      pattern: "PERMISSION-MODE"
    - from: ".planning/RELEASE.md"
      to: "tests/FRESH-INSTALL.md"
      via: "sign-off references fresh-install run"
      pattern: "FRESH-INSTALL"
    - from: ".planning/marketplace-pr.md"
      to: ".planning/marketplace-entry.json"
      via: "PR body embeds the JSON snippet"
      pattern: "alo-labs/claude-plugins"
---

<objective>
Wave 3 / final wave of Phase 7. Three checkpoints punctuate this plan because the work fundamentally requires the human-in-the-loop:
1. Score the 40-prompt activation panel (D-01) — only the human can run /plugin and observe activation routing
2. Run the 5-skill × 2-permission-mode matrix (D-02)
3. Run the 6-step fresh-machine install validation (D-06) on Mac + Windows
4. Sign the v0.1.0 RELEASE.md and approve the marketplace PR + tag push

Purpose: SC#1 (activation ≥8/10), SC#2 (allowed-tools in default + dontAsk), SC#4 (marketplace PR + tag + fresh-install), DIST-03 (PR landed), DIST-06 (semver tag), DIST-01 (description quality verified empirically).

Output: Manual-checklist docs (PERMISSION-MODE.md, FRESH-INSTALL.md), filled activation-results.md, signed RELEASE.md, drafted marketplace PR body + JSON snippet, prepared v0.1.0 tag command. Atomic commits per artifact group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-marketplace-release/07-CONTEXT.md
@.planning/phases/07-marketplace-release/07-01-PLAN.md
@.planning/phases/07-marketplace-release/07-02-PLAN.md
@.planning/ROADMAP.md
@CLAUDE.md
@.claude-plugin/plugin.json
@README.md
@tests/activation-panel.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author manual-checklist docs (PERMISSION-MODE.md + FRESH-INSTALL.md + marketplace PR draft)</name>
  <files>tests/PERMISSION-MODE.md, tests/FRESH-INSTALL.md, .planning/marketplace-pr.md, .planning/marketplace-entry.json</files>
  <action>
    Create the three manual-verification docs and the marketplace PR draft. These are filled-in templates the human runs against in the next task's checkpoint.

    **`tests/PERMISSION-MODE.md`** per D-02:
    - H1 + intro: purpose (validate `allowed-tools` scoping in `default` and `dontAsk` modes — NOT `bypassPermissions`).
    - Setup section: how to switch Claude Code permission mode (settings panel reference).
    - Per-skill × per-mode matrix (5 skills × 2 modes = 10 rows):
      ```
      | Skill | Mode | Invocation | Tool calls pre-approved? | Prompts cleanly? | Pass? | Notes |
      ```
    - Skills: `/instadecks:create`, `/instadecks:review`, `/instadecks:content-review`, `/instadecks:annotate`, `/instadecks:doctor`.
    - Modes: `default`, `dontAsk`.
    - Per-row invocation: pick the simplest representative prompt per skill that exercises every entry in its `allowed-tools`. E.g., `/annotate` row should hit `Bash(soffice:*)` + `Bash(node:*)`; `/create` should hit `Bash(node:*)` + `Bash(soffice:*)` + `Bash(unzip:*)` + `Bash(xmllint:*)`.
    - Pass criterion: in `default` mode the user is prompted ONLY for the scoped tools listed in frontmatter; in `dontAsk` mode no prompts appear and tool calls succeed. Any prompt for `Bash(*)` or any tool NOT in frontmatter = FAIL.
    - Aggregate pass row at end: `5/5 skills × 2/2 modes = 10/10 ✅` (filled by tester).

    **`tests/FRESH-INSTALL.md`** per D-06:
    - H1 + intro: validate fresh-machine end-to-end install per SC#4.
    - Two columns: Mac, Windows. (Linux deferred per CONTEXT.md "Out of Scope".)
    - 6 numbered steps mirroring D-06 exactly:
      1. `/plugin marketplace add alo-exp/instadecks && /plugin install instadecks`
      2. `/instadecks:doctor` reports all-green (or per-OS prereq install hint)
      3. `/instadecks:create` produces a valid deck from canonical brief: `tests/fixtures/canonical-brief.md` (if doesn't exist, embed brief inline: 5-bullet brief about "AI in healthcare" — short enough to inline)
      4. `/instadecks:review` produces findings JSON sidecar against the deck from step 3
      5. `/instadecks:annotate` produces annotated PPTX + PDF against deck + findings JSON
      6. Open all generated decks in REAL Microsoft PowerPoint (Mac + Windows separately) — no error dialog, no "PowerPoint repaired this file"
    - Each step has a Pass / Fail / Notes column per OS.
    - Footer: paste artifact paths into RELEASE.md after run.

    **`.planning/marketplace-entry.json`** per D-05:
    ```json
    {
      "name": "instadecks",
      "source": { "source": "github", "repo": "alo-exp/instadecks" },
      "category": "productivity",
      "version": "0.1.0",
      "description": "Generate, review, and annotate polished presentation decks. Four slash skills powered by pptxgenjs + DECK-VDA design review + auto-refine convergence loop."
    }
    ```

    **`.planning/marketplace-pr.md`** — PR body draft for alo-labs/claude-plugins:
    - Title: `Add instadecks plugin v0.1.0`
    - Body sections:
      - **Summary** (3 bullets): what it does (4 user skills + doctor self-check), what it ships (verbatim v8 BluePrestige annotate.js + DECK-VDA review + auto-refine loop), how it's licensed (Apache-2.0, all bundled deps MIT or SIL OFL).
      - **JSON entry** — embed the marketplace-entry.json contents in a code block.
      - **Testing evidence** — bullet list referencing:
        - tests/activation-results.md (40 prompts, ≥8/10 per skill)
        - tests/PERMISSION-MODE.md (10/10 default + dontAsk)
        - tests/FRESH-INSTALL.md (6/6 Mac + 6/6 Windows)
        - .planning/RELEASE.md sign-off
      - **License compliance** — bullet: license-checker zero-GPL, NOTICE + LICENSE bundled-software reconciled, per-dep licenses/ dir.
      - **Reviewer notes** — annotate.js relicensed by author from internal Sourcevo work; DECK-VDA methodology authored by Sourcevo and re-expressed under Apache-2.0; design-ideas guidance original to project.
      - **Tag** — references v0.1.0 (https://github.com/alo-exp/instadecks/releases/tag/v0.1.0).
      - Footer: `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.

    Atomic commit: `docs(07-03): add PERMISSION-MODE + FRESH-INSTALL checklists and marketplace PR draft per D-02/D-05/D-06`
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/instadecks &amp;&amp; test -f tests/PERMISSION-MODE.md &amp;&amp; test -f tests/FRESH-INSTALL.md &amp;&amp; test -f .planning/marketplace-pr.md &amp;&amp; node -e "const j=require('./.planning/marketplace-entry.json'); if(j.name!=='instadecks'||j.version!=='0.1.0'||j.source.repo!=='alo-exp/instadecks')process.exit(1)" &amp;&amp; grep -q "default" tests/PERMISSION-MODE.md &amp;&amp; grep -q "dontAsk" tests/PERMISSION-MODE.md &amp;&amp; grep -q "/plugin install" tests/FRESH-INSTALL.md</automated>
  </verify>
  <done>
    - All 4 docs exist
    - marketplace-entry.json validates as parseable + matches D-05 verbatim
    - PERMISSION-MODE.md has 10-row matrix
    - FRESH-INSTALL.md has 6 numbered steps × Mac/Windows columns
    - PR draft references all evidence docs by path
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human runs activation panel + permission-mode + fresh-install verifications</name>
  <what-built>
    Three manual-test scaffolds from Task 1:
    - tests/activation-panel.md (40 prompts ready to score from Plan 07-01)
    - tests/PERMISSION-MODE.md (10-row matrix)
    - tests/FRESH-INSTALL.md (6 steps × 2 OS)
    Plus 5 skills shipping (annotate, review, content-review, create, doctor) and audit gates (audit-allowed-tools.js, license-audit.js).
  </what-built>
  <how-to-verify>
    Run all three manual checklists in order. The agent CANNOT do this alone — /plugin activation matching, permission-mode prompts, and PowerPoint-on-Mac-and-Windows opening are all human-only verifications.

    **Step A — Activation panel (D-01):**
    1. Open Claude Code with the instadecks plugin installed.
    2. For each of the 40 prompts in tests/activation-panel.md, paste it into Claude Code as a fresh message and observe which skill (if any) activates.
    3. Score 1 if the correct skill activates, 0 otherwise. Fill tests/activation-results.md.
    4. Gate: each skill ≥ 8/10. If any skill < 8/10, return failure with notes (the description string in that skill's SKILL.md needs revision — escalate to a follow-up plan; DO NOT silently accept).

    **Step B — Permission-mode matrix (D-02):**
    1. Set Claude Code permission mode to `default`.
    2. Run the canonical-prompt invocation for each of the 5 skills. Observe: do the tool prompts list ONLY the scoped Bash entries (e.g., `Bash(node:*)`, `Bash(soffice:*)`) — never `Bash(*)` or unscoped tool names?
    3. Switch permission mode to `dontAsk`. Re-run the same 5 invocations. Confirm zero prompts appear AND every tool call succeeds.
    4. Fill the 10-row matrix in tests/PERMISSION-MODE.md. Gate: 10/10 pass.

    **Step C — Fresh-machine install (D-06):**
    1. On a fresh machine (or `rm -rf ~/.claude/data/plugins/instadecks` + restart) run the 6 steps in tests/FRESH-INSTALL.md.
    2. CRITICAL: open every generated PPTX in REAL Microsoft PowerPoint on Mac AND Windows (not LibreOffice — that's already what soffice uses). No "file repaired" dialog, no missing fonts, no broken layouts.
    3. Fill the Mac + Windows pass/fail columns. Gate: 6/6 on both columns.

    **Resume signal:**
    - If all three gates pass: type `verifications-passed` and paste the filled-in result paths.
    - If any gate fails: type `verification-failed: <step-A|B|C>` with notes; this triggers a follow-up plan to fix the failing item before proceeding to Task 3.
  </how-to-verify>
  <resume-signal>Type `verifications-passed` (with paths to the 3 filled docs) OR `verification-failed: <which step>` with notes</resume-signal>
</task>

<task type="auto">
  <name>Task 3: .planning/RELEASE.md sign-off + tag prep</name>
  <files>.planning/RELEASE.md</files>
  <action>
    Author `.planning/RELEASE.md` as the v0.1.0 sign-off log. ONLY runs after Task 2's checkpoint passes — the doc references the filled-in result files, so the agent reads them and embeds the evidence.

    Structure:
    - **H1: Instadecks v0.1.0 Release Sign-Off**
    - **Date** + **Signed-off by** (Shafqat Ullah / Sourcevo).
    - **§1 Success Criteria evidence:**
      - SC#1 — activation panel: link to tests/activation-results.md + paste the per-skill aggregate row (e.g., "create 9/10, review 10/10, content-review 8/10, annotate 9/10").
      - SC#2 — permission modes: link to tests/PERMISSION-MODE.md + paste 10/10 aggregate.
      - SC#3 — license compliance: paste output of `node tools/license-audit.js` (exit 0) and `npx license-checker --production --summary`.
      - SC#4 — fresh install: link to tests/FRESH-INSTALL.md + 6/6 Mac + 6/6 Windows aggregates.
      - SC#5 — README + doctor self-check: link to README.md sections + paste a sample `bash skills/doctor/scripts/check.sh` green output.
    - **§2 Audit-tool greens:**
      - `node tools/audit-allowed-tools.js` — exit 0
      - `node tools/license-audit.js` — exit 0
      - `bash tools/lint-paths.sh` — exit 0
      - `node tools/assert-pptxgenjs-pin.js` — exit 0
      - `node tools/lint-pptxgenjs-enums.js` — exit 0
      - `node --test` — all passing
    - **§3 Marketplace PR + Tag:**
      - PR draft: link to .planning/marketplace-pr.md
      - JSON entry: link to .planning/marketplace-entry.json
      - **Tag command (HUMAN runs after this doc lands):**
        ```bash
        cd /Users/shafqat/Documents/Projects/instadecks
        git tag -a v0.1.0 -m "v0.1.0 — first marketplace release; see .planning/RELEASE.md"
        git push origin v0.1.0
        ```
      - **Marketplace PR (HUMAN runs after tag):**
        ```bash
        gh pr create --repo alo-labs/claude-plugins \
          --title "Add instadecks plugin v0.1.0" \
          --body-file /Users/shafqat/Documents/Projects/instadecks/.planning/marketplace-pr.md
        ```
    - **§4 Post-merge actions:**
      - Update README badge URLs once CI workflow is wired
      - Capture the merged PR URL back into this RELEASE.md as the immutable record
    - **§5 Phase 7 plan history** — link to 07-01-SUMMARY.md, 07-02-SUMMARY.md, 07-03-SUMMARY.md.

    Run all the audit commands listed in §2 and embed their actual stdout (or `exit 0` confirmation) — do not promise green, prove it.

    Atomic commit: `docs(07-03): sign v0.1.0 release log + prep tag and marketplace PR commands`
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/instadecks &amp;&amp; node tools/audit-allowed-tools.js &amp;&amp; node tools/license-audit.js &amp;&amp; bash tools/lint-paths.sh &amp;&amp; node tools/assert-pptxgenjs-pin.js &amp;&amp; node tools/lint-pptxgenjs-enums.js &amp;&amp; node --test 2>&amp;1 | tail -5 &amp;&amp; test -f .planning/RELEASE.md &amp;&amp; grep -q "v0.1.0" .planning/RELEASE.md &amp;&amp; grep -q "git tag -a v0.1.0" .planning/RELEASE.md &amp;&amp; grep -q "alo-labs/claude-plugins" .planning/RELEASE.md</automated>
  </verify>
  <done>
    - All audit commands exit 0 (live output embedded in §2 of RELEASE.md, not just claimed)
    - .planning/RELEASE.md contains v0.1.0, the tag command, and the marketplace PR command
    - SC#1..SC#5 evidence sections each reference the corresponding test doc by path
    - Atomic commit landed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human approves tag push + marketplace PR submission</name>
  <what-built>
    Signed .planning/RELEASE.md with all SC#1..SC#5 evidence + audit-tool greens + the prepared (but not yet executed) `git tag -a v0.1.0` and `gh pr create` commands. Marketplace PR body draft (.planning/marketplace-pr.md) and JSON entry (.planning/marketplace-entry.json) ready.
  </what-built>
  <how-to-verify>
    The agent has prepared but NOT executed the destructive release operations:
    - `git tag -a v0.1.0 ...` (creates immutable release tag)
    - `git push origin v0.1.0` (publishes tag to GitHub)
    - `gh pr create --repo alo-labs/claude-plugins ...` (opens PR in external repo — not just instadecks)

    Read .planning/RELEASE.md end-to-end and confirm:
    1. SC#1..SC#5 evidence is real (open the linked tests/*.md and verify aggregates match)
    2. Audit commands listed in §2 actually exited 0 (re-run any you want to spot-check)
    3. The tag command and PR command in §3 are exactly what you want executed
    4. .planning/marketplace-pr.md PR body reads correctly (no broken links, no leaked TBDs)
    5. .planning/marketplace-entry.json matches D-05 verbatim

    **Resume signals:**
    - `release-approved` — the agent then runs `git tag -a v0.1.0 -m "..." && git push origin v0.1.0` and `gh pr create --repo alo-labs/claude-plugins ...`, captures the PR URL, and writes it back to RELEASE.md §4.
    - `release-blocked: <reason>` — the agent stops; the user fixes the issue and re-runs Task 3.
    - `release-deferred` — the agent commits RELEASE.md as-is; the user runs the tag + PR commands manually at their own cadence.
  </how-to-verify>
  <resume-signal>Type `release-approved` (agent executes tag push + PR create), `release-blocked: <reason>`, OR `release-deferred` (commands left for human to run)</resume-signal>
</task>

</tasks>

<verification>
- All 3 manual-checklist docs exist and are filled in (activation-results, PERMISSION-MODE, FRESH-INSTALL)
- .planning/RELEASE.md exists with §1..§5; audit commands embedded with real exit-0 output
- .planning/marketplace-entry.json parseable + matches D-05
- .planning/marketplace-pr.md drafted with all 6 body sections
- All audit-tool gates green (audit-allowed-tools, license-audit, lint-paths, assert-pptxgenjs-pin, lint-pptxgenjs-enums, node --test)
- Activation panel ≥8/10 per skill (4/4 skills pass gate)
- PERMISSION-MODE matrix 10/10
- FRESH-INSTALL 6/6 Mac + 6/6 Windows
- After Task 4 release-approved: v0.1.0 tag pushed; marketplace PR URL captured in RELEASE.md §4
</verification>

<success_criteria>
- DIST-01 / SC#1 description-quality verified empirically (≥8/10 per skill)
- DIST-02 / SC#2 allowed-tools scoping verified in `default` AND `dontAsk` (10/10)
- DIST-03 / SC#4 marketplace PR drafted, ready (or submitted on `release-approved`) to alo-labs/claude-plugins
- DIST-06 / SC#4 v0.1.0 semver tag prepared (or pushed on `release-approved`)
- SC#4 fresh-machine install validated end-to-end on Mac + Windows
- All Phase 7 success criteria SC#1..SC#5 closed with evidence in RELEASE.md
- Three atomic commits across Tasks 1, 3 (Tasks 2 & 4 are checkpoints, no commit); +1 commit if Task 4 lands the release-approved tag/PR result back into RELEASE.md
</success_criteria>

<output>
After completion, create `.planning/phases/07-marketplace-release/07-03-SUMMARY.md` per the GSD summary template, including:
- Manual-checklist result aggregates (activation, permission-mode, fresh-install)
- RELEASE.md path + signed-off date
- Marketplace PR URL (if release-approved was the resume signal)
- v0.1.0 tag SHA (if pushed)
- Phase 7 closure note: roadmap can be marked `[x] **Phase 7: Marketplace Publication & Release Polish** *(complete YYYY-MM-DD — 3/3 plans, DIST-01..DIST-08)*`
</output>
